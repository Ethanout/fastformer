# 扫掠 / 放样临时建议

## 结论

- 道路首要对象不是横断面，而是“已建道路片段模板 + 目标路径”的变形放样。
- 异形屋顶用“多层闭合轮廓”放样。
- 两者只共享局部坐标、路径/截面采样和体素化；不要合成一个万能工具。

## 道路扫掠

目标：对标 Axiom 曲线，但不要求导出结构文件再重新选择。

首版把选中的所有方块视为一个可变形结构，不区分路面、路灯和花丛。先追求“快”，附属物策略以后再加。

首版交互：

1. 用现有操作会话选中一段已建结构。
2. 直接点“曲线变形”；选区留在内存，不保存文件。
3. 默认用选区最长轴作为结构纵向；只有不满意时才点轴 Gizmo 改变方向。
4. 直接连续点选目标曲线，双击结束。
5. 立即显示变形后的虚影，Enter 确认为一个可撤回批次。

理想的用户路径只有：

`选区 → 曲线变形 → 画曲线 → Enter`

几何规则：

- 将选区沿纵向归一化到 `0..1`，再映射到曲线参数 `0..1`；横向和竖向坐标跟随曲线局部框架。
- 首版允许整体拉伸，接受灯杆、花丛随结构一起变形；不先引入重复间距、附属物标记或组编辑。
- 默认使用“切线 + 世界上方向”的稳定框架，避免曲线拐点翻转。
- 预览先画边界和稀疏骨架，曲线停止输入后再补全方块，保持操作流畅。
- 放置阶段一次性提交方块差异，不逐块发送交互请求。

可选的一个小开关：`拉伸整个选区` / `沿曲线重复选区`。不要在首版加入更多道路专用参数。

这个功能更准确的名字是“选区曲线变形”。

## 屋顶放样

输入：檐口轮廓、若干中间轮廓、顶部轮廓或顶点。

好时巧克力形屋顶：底部闭合轮廓 + 若干逐渐缩小、升高的闭合轮廓 + 顶点。

首版交互：

1. 先确定檐口闭合轮廓。
2. 复制截面并用 Gizmo 调高度、缩放、偏移、旋转。
3. 最后一层允许收束成单点。
4. 预览轮廓环和纵向肋线。

几何规则：

- 每层轮廓必须同拓扑；顶点是唯一特例。
- 沿周长归一化重采样，并固定接缝点，防止层间扭转。
- 首版使用线性插值；平滑曲线作为显式选项。
- 屋面厚度沿局部法线生成，不按世界 Y 轴硬加。

## 需要先定的选项

- 道路默认贴地，还是默认保持设计标高。
- 道路转角只要倒角，还是还需要圆角。
- 屋顶截面默认线性，还是允许平滑插值。
- 放样是否允许层间旋转；允许时必须显示接缝和扭转预览。
