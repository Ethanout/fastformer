# 2026-09-16 14:03 审计验证状态

整体审计未完成。以下是已执行检查的结果，不代表之后的并发改动已经验证。

| 检查 | 已观察结果 | 能证明的范围 |
| --- | --- | --- |
| Gradle 单元测试 | 226 个测试类，1228 项，0 失败，0 错误，1 跳过 | 覆盖空工作区撤销漏修后的编译与测试 |
| GameTest 服务端 | 50 项完成，18 项必需测试失败，游戏进程退出码 18，Gradle 退出码 1 | 模组能够启动，但运行时测试未通过 |
| 客户端游戏验收 | 未执行 | 不能证明按键、HUD、渲染或帧率正常 |

## GameTest 失败证据

命令：`.\gradlew.bat runGameTestServer --no-configuration-cache --console=plain`。

本次服务端在 14:02:51 报告测试结束。完整日志保存在 `.dsh-tmp/gametest-20260916-1402.log`，避免下一次运行覆盖。

首个失败是 `managerloadsolderhistorypagebeforemultiundo`，原因文字为 `manager did not accept the retried multi-undo`。后续失败包括取消、恢复、放置等路径等待写入许可或准备完成。是否存在共享状态污染尚未确认，不能把 18 项失败直接等同于 18 个独立 bug。

日志还包含文件访问和损坏数据异常。部分测试会主动注入故障，必须结合测试断言判断，不能按异常数量计算缺陷。

## 未完成的工作

- 源遮罩仍有修改客户端世界状态的旧实现。渲染层遮罩重构尚未交付。
- 提交等待 HUD 已确定修复范围，尚未完成验证。
- 提交生命周期、旋转状态、外框顺序和生成预算仍需交付与复核。
- 草稿边界审计发现复制入口缺少生成预算。渲染与服务端使用不同上限本身不构成缺陷。
- 18 项 GameTest 失败正在定位。修复后必须重新运行相关运行时测试。

历史验证记录保留供追踪。旧的成功记录不能覆盖本次失败，也不能证明后续代码。

文本检查：STE 分数 0.00 / 100 词。英文检查器对中文覆盖有限。
