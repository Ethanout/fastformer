# 几何缓存生命周期

帧缓存和解析缓存使用弱部件引用，但弱引用清除后仍可能留下 map 条目和已解析方块值。若会话没有正常断开，单靠固定容量会延长这些值的保留时间。

新增 `prunePartGeometry`，每个客户端 tick 清理已被垃圾回收的部件，并同步修正解析方块计数。工作区清空和断线仍调用完整失效。缓存容量、总方块预算和弱引用共同限制长期保留。

全量 `gradlew test --console=plain` 在该修改后成功。客户端实际垃圾回收和长会话帧时间仍需实机观察。
