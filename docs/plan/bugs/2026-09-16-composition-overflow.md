# 几何预算的整数边界

日期：2026-09-16。状态：边界修复完成，相关定向测试通过。完整预算迁移尚未完成。

发现：缩放尺寸先从 long 转 int，可能把大尺寸绕回小尺寸。三维扫描体积相乘也可能溢出。重复遍历达到访问上限后，调用方未检查是否遍历完全部重复单元。

修复：缩放尺寸保留 long，转 int 前检查范围。体积先用除法检查工作预算，再执行乘法。未完成的重复遍历返回 WORK 超限结果，不返回部分成功内容。原有无需缩放的稀疏输入不扫描包围盒。

新增测试覆盖尺寸 4294967297 和体积 2097152 的三次方，要求在扫描前拒绝。预算测试与全部 WorkspacePreviewComposer 测试共 25 项通过，无失败、错误或跳过。`git diff --check` 通过。此次结果不能证明全部交互调用链或整个模组已完成验证。

DSH 的 Grok 中转因余额不足停止历史任务，其他任务也出现中断。主代理接手本项修复。其余未交付改动仍需审查。

文档检查：STE 0.00 / 100w，中文覆盖有限。
