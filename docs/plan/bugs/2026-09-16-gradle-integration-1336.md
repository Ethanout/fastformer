# 2026-09-16 13:36 集成测试记录

## 当前证据

- 主代理运行：`.\gradlew.bat test --no-configuration-cache --console=plain`。
- 结果：`BUILD SUCCESSFUL in 30s`，进程退出码为 0。
- `compileJava`、`compileTestJava`、`test` 均执行，不是仅沿用旧测试报告。
- `build/test-results/test/TEST-*.xml` 汇总：226 个测试类，1221 项测试，0 失败，0 错误，1 跳过。
- 原先撤销测试引用旧接口导致的编译错误，本次未再出现。

## 证据边界

这次结果仅覆盖本次 Gradle 所编译的代码。其他代理仍在同一工作区修改文件，后续改动需要重新测试。

DSH 自行运行的 JUnit 报告含 12 项失败，并报告 Guava 类路径冲突。该运行与本次 Gradle 运行不同，不能混合统计。主代理未独立复现其类路径根因。

测试通过不证明游戏内撤销按键、拖拽反馈、渲染遮罩、Mixin 注入或帧率符合要求。源遮罩架构、提交生命周期和旋转路径仍有未完成工作，整体审计未完成。

## 通信检查

主代理发送 `bidir-check-20260916-undo-1`，DSH 接受后向当前对话返回同一标识。此次双向往返检查通过。

## 文本检查

STE 文本检查分数：0.00 / 100 词。检查器只识别了少量英文词，不能据此证明中文表达质量。
