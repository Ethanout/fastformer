# 首次历史读取失败的保护与重试

日期：2026-09-16。局部修复通过定向测试，历史调度整体未完成。

问题：首次快照加载失败标志只在内存缓存为空时阻塞操作。但合并过程会先创建缓存，缓存存在不能证明加载成功。

修复：加载失败标志独立阻塞操作。再次请求撤销或重做时可触发一次快照读取，已有读取任务时不重复提交。成功合并后才清除失败标志。设置刷新也可重试失败读取。

验证：WorldHistoryOwnerLifecycleTest 和 HistoryTaskSchedulerTest 通过。新增回归覆盖已有缓存时加载失败仍阻塞、重试期间仍阻塞、成功合并后解除阻塞。测试没有覆盖玩家输入触发的真实磁盘重试。

限制：本项未解决批量撤销超过缓存容量时的重复分页问题，也未完成分页失败后的完整执行顺序验收。

文稿检查：STE 0.00，中文覆盖有限。

入口复查：首次快照失败后的重试原先被输入层的忙碌判断拦截。撤销和重做入口现在先识别可重试的快照失败，再交给历史管理器处理。正在加载时仍保持阻塞。

输入分发及历史相关单元测试通过。日志：`.dsh-tmp/history-input-review-20260916.log`。仍需玩家输入与真实磁盘故障的集成测试。
