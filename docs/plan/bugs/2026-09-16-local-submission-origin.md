# 纯剪贴板提交的来源一致性

类别：程序逻辑、断线恢复体验。

纯剪贴板工作区提交时，回执会清除服务端选区身份，但草稿构建优先采用仍活跃的选区身份，导致回执和草稿来源不一致。重连恢复可能错误要求无关服务端选区匹配。

修复统一提交入口、任务跟踪与草稿的来源：本地提交不携带服务端选区身份。回归测试覆盖本地提交同时存在旧选区身份的情况。

`ClientPlayerSessionTest` 与 `OperationSubmissionReceiptTest` 定向测试通过，构建耗时 9 秒。断线恢复端到端测试仍待完成。

文案检查得分为 0.00，中文覆盖有限。
