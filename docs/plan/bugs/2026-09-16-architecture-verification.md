# 架构修复验收记录

日期：2026-09-16。整体状态：未完成。

## 当前验证基线：集合与缓存修复之后

2026-09-16 20:36 的 GameTest：81 项必需测试全部通过，构建用时 31 秒。日志：`.dsh-tmp/gametest-collection-cache-20260916.log`。

随后完整单元测试：1451 项，1450 项通过，0 失败，0 错误，1 项跳过，构建用时 27 秒。日志：`.dsh-tmp/unit-collection-cache-20260916.log`。

唯一跳过项仍为 `GroupRepeatTest.placedCopiesSurviveACommonSecondStackAfterPerPartFirstStacks`。混合重复步距的公共堆叠架构尚未完成。

本轮没有修改程序逻辑。验证包含此前累计的历史分页、写入暂停时丢弃分页、公共手势反馈、批次预算、坐标集合性能、同步网格缓存提交和外壳拒绝清理修改。

这些测试不能证明客户端实机显示、帧耗时、峰值内存或全部网络时序正确。当前仍需源引用与目标格网分离、服务端累计工作量预算、历史分页失败后的剩余数量恢复，以及持续渲染失败退避。

架构实施前的当前证据：`OperationWorkspaceValidator` 仍用同一份 `part.blocks()` 同时取得 WORLD 清除键和目标展开输入。`ClientOperationDraft.CURRENT_VERSION` 仍为 1。因此不能只把变换结果写回 blocks 来修复分组堆叠，否则会改变源清除含义。必须一起调整部件模型、提交协议、草稿兼容读取与验收测试。

文稿检查：STE 0.00，中文覆盖有限。

## 历史验证基线：1428 项单元测试

本轮完整单元测试：1428 项，1427 项通过，0 项失败或错误，1 项跳过。用时 22 秒。结果位于 `build/test-results/test`。跳过项仍为分组重复架构问题。

本轮 GameTest：79 项必需测试全部通过，命令用时 31 秒。结果日志位于 `run/gameTestServer/logs/latest.log`，19:29:01 的结果记录确认数量。

本轮包含有界组合、整数溢出、单部件手势范围、LOCKED 手柄命中与预览失败重试的当前实现。预算调用链审查和历史调度修复尚未完成。测试通过不代表整体审计完成。

文稿检查：STE 0.00，中文覆盖有限。

### 前一轮基线

- 全量单测：1411 项，1410 项通过，0 项失败，1 项跳过。Gradle 用时 28 秒。日志：`.dsh-tmp/unit-cancel-review-20260916.log`。
- 跳过项仍是混合点阵的分组重复目标行为。该架构问题尚未修复。
- 全量 GameTest：79 项必需测试全部通过，包含 6 项旋转状态测试和 4 项取消回执测试。最终一轮用时 29 秒。日志：`.dsh-tmp/gametest-cancel-rotation-review-20260916-r3.log`。
- 新增取消测试最初有编译错误，以及错误的空目标假设。均已修正。模板尺寸实际为 1×1×1，新测试最终使用模板原点，并显式准备目标状态。恢复用例验证真实方块写入和还原，完成用例只接受成功。
- 此基线包含预算结果类型和限制常量，但不包含尚未实现的有界组合流程。后续预算改造需要重新验证。

本节文稿检查：STE 0.00；中文检查覆盖有限。

## 早期编译检查（已解决）

旋转实现与测试已冻结。随后运行全量单测命令，但在 compileJava 阶段失败，尚未执行测试。新增 OperationCancelSettlementGameTests 有三处编译错误：继承 final 的 WorldJournalPreparation，以及两处跨包调用不可访问的 addTaskForTest。负责代理正在修正测试结构。

日志：`.dsh-tmp/unit-rotation-review-20260916.log`。此前通过数量属于历史结果，不能证明当前工作树通过。

本轮差异格式检查通过。中英文语言文件各有 380 个键，键集合一致；这不证明所有交互均有必要提示。

本节文稿检查：STE 0.00；中文检查覆盖有限。

## 此前验证

- 全量单测：1410 项，1398 项通过，11 项失败，1 项跳过。Gradle 用时 29 秒。
- 全量 GameTest：69 项必需测试全部通过。Gradle 用时 29 秒。日志：`.dsh-tmp/gametest-admission-review-20260916.log`。
- 这轮 GameTest 包含准入重放、请求域、通知故障、写入门闸和恢复快照收尾。旋转状态的真实方块测试尚未迁入，不能用这份结果证明旋转正确。
- 此前客户端回执与重复区域定向单测 104 项全部通过，无跳过。
- 回执断言修正后，`WorkspaceAdmissionTest` 的 18 项定向单测全部通过，无跳过。全量单测尚未重新运行。

## 单测失败与后续修复

1. `VoxelRotationSnapshotTest`：7 项失败。6 项因普通单测运行时未初始化 Minecraft 注册表，需移至 GameTest。另 1 项发现两格直线旋转 90 度后实际变成三格，需要检查整数边界处的浮点误差与桥接补格，不能直接改期望值使测试通过。
2. `WorkspacePreviewComposerCacheTest`：3 项失败。旧夹具用 Unsafe 创建了没有方块状态的快照，新类型化旋转会访问状态。需要修夹具，不能给生产代码加空状态容错来掩盖。
3. `WorkspaceAdmissionTest`：1 项失败。UNKNOWN 不发送结果也不保留请求域，旧测试错误地将两者视作互补。
4. `GroupRepeatTest`：1 项明确禁用的目标行为测试。两个部件先各自重复，再共同重复时，当前单层步距模型会重排已有副本。这是未修复的架构问题，见 `2026-09-16-group-repeat.md`，不能计为覆盖完成。

## 已纠正的测试问题

新增 GameTest 曾将 `succeedWhen` 的 Runnable 写成返回布尔值，导致编译失败。等待现在用断言表达，并保留一次性阶段的失败，避免重试时跳过失败断言而误成功。测试收尾改为清理自己的记录。69 项结果来自这些改动之后。

## 静态排除项

中英文语言文件各有 380 个键，键集合及格式占位符一致。代码直接引用的提示键均存在。未匹配项只有动态前缀、测试键和进程测试标志。此检查不证明所有交互都有必要提示，也不验证动态生成的每个键。

当前编译依赖的渲染区字节码同时含四参数和五参数构造器。四参数构造器委托五参数构造器，现有遮罩注入因此覆盖两条构造路径。此证据排除漏绑四参数构造器的疑点，不替代客户端启动及实际渲染验证。

证据来自 `build/neoForm/neoFormJoined1.21.1-20240808.144430/steps/recompile/outputs.jar` 的 `javap -c -p` 输出，以及本轮语言键和占位符比较。本节文稿检查：STE 0.00；中文检查覆盖有限。

## 未完成

客户端渲染的实际显示、异步网格更新与取消仍需实机验收。几何组合的输出预算与工作量预算正在实现。取消任务的终态修复已通过上述 GameTest；网络发送次数和客户端实机行为仍未验证。

文稿检查：STE 0.00；中文检查覆盖有限。
