# 待审阅设计问题

本页记录尚未收到用户答案的问题，以及继续实现所采用的暂定方案。用户于 2026-09-20 授权在未回复时采用合理方案继续工作。暂定方案不是用户确认，不能覆盖已确认伪代码。收到答案后，同步更新对应伪代码、实现和本页。

## 仍待确认

| 问题 | 当前依据与暂定方案 | 实现状态 |
| --- | --- | --- |
| 特殊形状完成后写世界还是进入选区会话？ | 保留现有直接写世界路径；不新增自动转部件行为。对应 sessions/03-特殊形状.md 的 D2。 | 尚未改动完成去向 |
| 特殊形状各阶段左键如何回退？ | 优先保留已有逐步撤回行为；按具体形状整理后分别列出差异。对应 D5。 | 待核对 |
| 快速起形取消时如何保留既有选区部件？ | 只取消本次起形草稿及其未发送意图，保留之前已有的选区部件和剪贴板。对应 D6。 | 完整生命周期迁移未完成 |

## 已确认，已写入伪代码

- 快速起形 Enter 默认丢弃预备点，只提交已确认部分。专门规则优先，目前唯一例外为滚轮模式。
- 提交数据计算中等待计算与校验完成后自动提交一次，Q 可取消未发送意图。
- 左键类似会话内 Ctrl+Z，逐级回退，多边面与多面体每次撤回一个点。
- 坐标平面／基准面中键确认第三点后立即写世界。多边面中键无操作。
- 选区交互的已确认规则集中于 sessions/10-选区.md，不在本页复制整份规范。
