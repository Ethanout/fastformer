# 当前 TODO

本文件只记录尚未验证、且会影响玩家体验或数据安全的事项。模组以小型结构操作为主，首个方块的落下延迟优先于超大操作的总吞吐。代码结构调整必须服务于这些结果。
当前任务完成后，检查 `docs\plan\feature_todo\2026-08-31-anti-failure.md`，继续更新任务，并将已完成的部分写入 `docs/changelog`。

已勾选条目见 [2026-09-10 归档](../changelog/2026-09-10-completed-todo.md)。

## 本轮已完成

以下是已完成的实现和自动化测试子任务。详细记录归档于上述 changelog；各项剩余验收继续列在下方。

- [x] 删除撤回、重做的冗余按住标记，消除任务期间漏收释放事件造成的残留状态。
- [x] 长方体整体移动只平移当前 `min/max`，保留输入点与推拉结果；测试覆盖负方向移动、撤回重做和重新设点。
- [x] 可编辑工作区虚线框使用完整选区范围；测试覆盖边缘空气、输入点与范围不一致、变换后的结果框。
- [x] 客户端和服务端放置射线统一按 `canBeReplaced()` 跳过可覆盖方块；GameTest 覆盖矮草、单层/双层雪、火把、目标位置和入射面。
- [x] 普通确认和快速起形增加 `SUBMITTING` 状态与请求 ID 回执；测试覆盖迟到预览、重复提交、错误回执、拒绝、取消和断线。
- [x] 增加真实 `system_chat` 网络编码回归测试，覆盖长字符串、非有限数值与大坐标参数。

以上修改通过 `check`。本轮 `runGameTestServer` 通过；这些结果不替代客户端画面、首块可见延迟和重启恢复验收。

## 体验验收

- [ ] 完成普通放置、工作区和选区各阶段状态、Q 取消恢复，以及换维度/退出游戏的客户端验收。归档中的实现记录不替代实机结果。



## 性能与资源

- [ ] 测量点击到首个方块、总耗时、单批 P95 和峰值堆内存。小型结构先按首次落块延迟排序。
- [ ] 单独测量小型操作的“首块等待”和“剩余批次写入”时间，区分快照/journal 等待与每 tick 写入预算。（日志已增加 `afterFirstWriteMs`，仍需相同场景实机采样）
- [ ] 单独设计并验证首块等待优化：让首批 before 快照与 journal 准备尽早开始，在首批 journal 就绪后立即写入；记录并比较“确认输入到首块”和“首块到完成”的两段延迟。（已避免已完成 journal future 的额外轮询 tick；首批分段 WAL 和客户端数据仍待验证）
- [x] 首块延迟专项子任务：把确认输入后的等待拆成入队、生成、首批快照、journal、写入许可五段，分别记录耗时和线程/调度归属。（普通放置、工作区、选区及历史任务已有各阶段时间和线程名指标；`.233` 实机采样仍待补齐）
- [ ] 首块延迟专项子任务：验证小结构（1、16、256、4096、32768 方块）在单次操作中是否能在一个或极少 tick 内落下首批；不得用提高全局预算掩盖前置等待。
- [ ] 首块延迟专项子任务：以“首块可见时间”为主要门槛，比较优化前后中位数与 P95；总耗时仅作为次要指标，避免为吞吐牺牲交互反馈。
- [x] 首块延迟实现专项：检查确认事件、任务入队、首批 journal 完成与主线程写入之间的 tick 边界；在安全条件满足后立即提交首批写入，避免仅因调度切换额外等待一个 tick，并用 1、16、256 方块场景验证。
- [ ] 用相同场景比较生成、快照、压缩和世界写入的等待时间。
- [ ] 确认 `32768`、`131072`、`262144` 和超过 `262144` 方块的放置与撤回行为。
- [ ] 内存不足时优先等待或减小批次。只有无法保留安全工作集时才拒绝操作。
- [x] 验证生成结果、快照、journal 和历史压缩不会无故重复保留完整数据。（`WorldChangeBatchTest` 已验证 dense/uniform 历史批次会省略重复位置和状态数组；生成到 journal 的全生命周期峰值仍待测量）

### 写入与历史分离子任务

- [x] 抽出写入期对象，只负责冲突检查、批次写入和实际结果收集。（已完成历史发布对象的第一步拆分；写入状态仍需兼容工作区与选区任务后再拆）
- [x] 让小型操作在必要的首批 before 与 journal 就绪后开始写入，不等待完整历史压缩。（历史压缩已后置；普通放置、工作区和选区已在首批 snapshot 后写入 `segment-000000` 并允许这些位置落块，后续 snapshot 追加 `segment-000001` 起。仍需相同场景实机比较首块延迟）
- [ ] 首块延迟专项子任务：将 manifest、segment、seal 接入实际写入路径，并补充迁移、崩溃恢复和重启清理测试。（写入路径已按首批/后续 segment 追加；完成时仍写 `seal.done`。单元测试覆盖追加序号、首批大小和 append poll。进程重启清理和持久保存验收仍未完成）
- [ ] 为取消、写入失败、journal 失败、历史失败、世界卸载和进程重启补充资源终态测试。（普通取消、生成失败预约释放、journal 取消竞态防护、恢复转移、journal 准备失败、未封存 journal 不发布历史、分段目录完整性门禁和无 journal 历史发布取消已有覆盖；世界卸载、进程重启后的真实资源终态仍待补齐）
- [ ] 用首个方块落下延迟和总耗时对比重构前后相同场景。

## 自动化覆盖

### 当前后续步骤

- [x] 修复分段容量判断、跨段最终状态修正，以及普通放置清理暂存后无法继续提交的问题。
- [x] 增加普通放置、工作区的真实成功和首块写入后取消测试，增加选区跨段复制与重叠移动测试。
- [x] 验证重叠移动在清除源区后、写入目标前可通过未封口日志恢复。
- [x] 补齐首段日志完成后的真实任务唤醒测试，包括预算边界和迟到回调。
- [ ] 补齐三类任务的写入异常、世界卸载/重载和进程重启矩阵。
- [x] 复核无变化操作的完成提示、提交阶段内存重试及资源终态。

### 资源与恢复补充子任务

- [x] 普通放置遇到临时预约竞争时等待，释放竞争预约后重试原任务。
- [x] 启动恢复逐段校验和读取，避免全部解码分段同时驻留。
- [x] 工作集预约等待 journal begin、append 与历史发布结束后释放。
- [x] 覆盖启动扫描、分段恢复、保存失败保留日志及重试、损坏日志和未知维度写入门禁。
- [x] 生成取消后等待实际 worker 退出再释放生成预约，不能把 future 取消当作 worker 停止。
- [x] 工作区和选区的增量快照与提交预约遇到临时竞争时可重试，不能重复累计快照预算。

上述已完成子任务的证据见 [2026-09-11 归档](../changelog/2026-09-11-completed-todo.md)。

此前验证：`gradlew --offline --no-daemon check` 与 `runGameTestServer` 均通过，GameTest 6/6 required tests passed。输出指标来自恢复操作，不证明普通放置的点击到首块延迟，也不替代客户端交互和生命周期验收。后续修改按各自的定向测试记录，不能沿用此前全量通过的结论。

2026-09-09 本轮验证：`check` 通过，`runGameTestServer` 7/7 通过。新增 `JournalRecoveryGameTests` 使用真实世界与日志文件，覆盖两条 after 修正的回滚和回放，修复修正记录尚未遍历完就误报不对齐的问题。该测试直接调用单日志恢复，不证明进程重启、启动扫描和持久保存清理全部完成。`WorldJournalPreparationCancellationTest` 覆盖取消前未调度、取消后的迟到完成与 reset、已交出日志保留。

追加验证：`runGameTestServer` 8/8 通过。新增分段目录 GameTest 覆盖 sealed after 回放、未封存 before 逆序回滚、重复执行和外部修改保留。该测试直接调用恢复入口，尚未证明真实进程重启。

- [x] 补齐普通放置、工作区和选区任务从成功、取消、写入异常到恢复创建的端到端组合测试。
- [ ] 覆盖重复位置、方块实体、流体、冲突、日志读写、世界卸载和重新加载。（重复位置、方块实体、流体恢复、恢复冲突以及日志读写/故障路径已有自动化覆盖；世界卸载/重载仍待补齐）

## 维护改进

- [x] 完成提交期间输入所有权收敛。普通确认和快速起形已在发包前进入 `SUBMITTING`，由匹配请求 ID 的服务端回执解除等待；测试覆盖旧预览、重复提交、错误回执、拒绝、取消与断线。选区应用、工作区传输和真实网络延迟下的交互验收仍待补齐。
- [x] 将预览状态、异步生成、命中判断和绘制建立清晰的所有权边界。（预览状态已拒绝迟到 revision；异步生成取消后会校验 `BuildingPreviewKey`，旧结果不会写回缓存；仍需客户端端到端验收）
- [x] 把 `WorldChangeTransaction` 收敛为写入期状态，另设小而明确的历史发布对象。不要让普通写入调用者负责决定何时压缩、提交或释放历史。
- [x] 对于在原版中右键点击可以直接覆盖上去的方块（如积雪、矮草），在快速起形或放置方块的 raycast 时将其视为空气。客户端与服务端放置射线已统一按 `canBeReplaced()` 跳过方块，选区/几何保留轮廓命中。GameTest 验证矮草、单层/双层雪、火把、表面目标位置和入射面；实际放置后的方块朝向仍待客户端验收。
- [x] 选区渲染、缩放、推拉面、命中测试和预览包统一以 `min`/`max` 权威范围为输入，不得把点1/点2当作范围角点。点1/点2只由左右键更新，并在更新后用于重算 `min`/`max`；中键扩展和其他范围变换不得改写点1/点2。已修复单点空值崩溃、整体移动丢失扩展范围和改写输入点的问题；长方体不再接受控制点移动命令。测试覆盖扩展后负方向移动、撤回重做和重新设点；复杂连续操作的客户端渲染、退化范围和坐标缩放仍待验收。
- [x] 排查复杂选区操作后的客户端强制退出：任何 FastFormer 状态消息都不得触发 `clientbound/minecraft:system_chat` 编码异常；校验消息参数、组件树、长度和异常路径，并补充网络编码回归测试。
- [ ] 近距离原版交互、单击、拖拽松开、确认后的迟到事件和 Alt 切换不会误触发 FastFormer 动作。输入状态机现在是所有键盘、原版交互、鼠标和滚轮事件的第一道路由；放置、恢复、取消阶段只允许取消事件，阶段切换会统一使旧手势令牌失效。Alt 在无 FastFormer 会话时已不再写入控制器状态，服务端有限距离 raycast 也已正确应用传入范围。仍需补充真实交互验收，并继续收敛动作分支内部的业务条件，避免把业务状态重新复制成第二套输入状态机。
- [x] Smart 木框架在线框模式的线和面阶段也能根据朝向生效。
- [x] Alt或中键等穿透选择时，应该使用首次击中面的平面作为方块被放置的面。

## 入口

- 交互：`client/input/`、`client/session/`、`client/render/`
- 放置：`fastplace/task/`、`fastplace/FastPlaceManager`
- 世界：`fastplace/world/`、`PersistentRecoveryJournal`
- 传输：`network/transfer/`、`FastPlaceNetwork`

运行验收项目使用 [runtime_acceptance.md](runtime_acceptance.md) 的步骤和证据要求；单元测试或 GameTest 不能替代其中的客户端与服务器生命周期验证。

已有记录称 GameTest 冒烟测试、惰性直线与事务顺序检查通过，开发客户端可以进入世界。这些记录不证明上述验收完成，也不替代当前版本的验证。

## 新增实机验收

- [ ] 在有选区草稿、锁定工作区或待提交请求时换维度、退出并重连，确认旧选区、预览、Alt 模式和提交等待均不会恢复；已写入方块的服务端恢复继续完成。

下列条目承接已勾选旧任务中的游戏内验收要求。旧条目的正文保留，勾选表示实现和自动化部分完成，不表示游戏内验收通过。

- [ ] 在 `.233` 采集普通放置、工作区、选区与历史任务的分阶段耗时和线程记录，确认日志可用于定位首块等待。
- [ ] 在相同场景比较首批 journal 就绪后的首次可见落块时间，确认普通放置、工作区和选区不等待完整历史压缩。
- [ ] 在积雪、矮草目标上执行快速起形和放置，检查最终方块位置与朝向，并确认火把不会被当作可覆盖方块跳过。
- [ ] 实机验收提交状态：普通确认、快速起形、选区应用和工作区分片提交后立即点击、滚轮、按 Alt 和 Q，确认不产生重复动作；服务端拒绝或传输超时后可以重新编辑，迟到回执不能恢复已退出会话。

- [ ] 实机验收 Smart 木框架：在线框模式分别完成线、面形状，检查预览与最终原木轴向一致，并检查关闭效果后恢复普通放置朝向。

- [ ] 实机验收 Alt/中键嵌入放置：从六个方向命中方块，检查楼梯、台阶和原木预览及最终朝向使用入射面，不随嵌入模式反转。

- [ ] 实机复测复杂选区写入失败及 Q 回退：确认错误消息正常显示，不再出现 system_chat 编码断线，并检查恢复完成后的世界状态。

- [ ] 在 `.233` 对比普通放置、工作区和选区各 1、16、256 方块的确认到首次可见落块延迟；另测 512 方块选区，确认首批不会等全部目标检查完成。服务端唤醒测试不代替客户端可见时间。

- [ ] 在 `.233` 连续执行选区设点、推拉、缩放、中键扩展、整体移动、撤回重做，检查框线和命中范围一致；覆盖反向范围、单点和退化范围，确认点1/点2只因明确设点操作改变。
- [ ] 用相同结构采样生成、快照、journal、提交各阶段的峰值堆和对象驻留，确认所有权清理测试所覆盖的释放时机在实际运行中生效。

- [ ] 实机验收预览：连续移动候选点、切换形状、取消会话及切换世界，确认旧异步结果不会闪回，取消大预览后帧时间恢复正常。

## 当前非实机后续子任务

- [x] 三类任务的容器移除回调抛异常后，通过真实管理器进入恢复，还原箱子及物品并释放内存预约和写入许可。
- [x] 工作区验证后冲突、KEEP_EXISTING 重叠移动取消时，恢复任务只还原自己拥有的修改，保留外部修改。
- [x] 三类部分写入任务通过管理器的“维度不可用”分支等待，再使用真实维度完成原任务。该测试模拟不可用上下文，不代替实际卸载/重载。
- [ ] 生成入队前的临时内存预约竞争仍会直接拒绝；应保留冻结的生成输入并等待，预约成功后启动一次生成，Q/关服可取消等待且不启动旧任务。
- [ ] 完成剩余输入动作分支的路由收敛，并检查同一会话下手势结束后的迟到事件。
- [ ] 补齐三类任务 journal/历史失败及关服资源终态，区分已覆盖的公开恢复扫描与真实服务器进程重启；不要用同进程 recoverAll 代替后者。
