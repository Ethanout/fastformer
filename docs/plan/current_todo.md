# 当前 TODO

本文件以当前回退版本为基线，记录重构任务和验收要求。用户确认旧版本的 bug 在当前回退版本中不存在，旧 bug 清单及对应版本的验收记录不再作为当前待办。历史过程记录见 `docs/changelog`。
模组以小型结构操作为主。预览跟手和首个方块反馈优先于超大操作的总吞吐。
当前任务完成后，继续检查 `feature_todo/2026-08-31-anti-failure.md`。

## 记录规则

2026-09-20 快速起形新增 Enter 异步生成校验与一次提交意图，Q 可取消未发送意图。实现范围和剩余限制见 [快速起形提交等待](../changelog/2026-09-20-quickshape-wait.md)。完整客户端候选流水线和实机验收仍未完成。

2026-09-20 阶段版本保存当前修复与职责拆分，完整集成通过 88 项 GameTest。范围与剩余边界见 [阶段版本记录](../changelog/2026-09-20-stage-checkpoint.md)。此版本不表示本 TODO 完成。

未回复的问题与暂定方案记录在 [待审阅设计问题](design_questions.md)。用户于 2026-09-20 授权未收到回复时按合理方案继续实现。确认后的答案同步写入会话伪代码。

- 用户报告先进入 TODO，不要求先复现。
- 自动化通过不等于客户端或服务器实机通过。
- 合并重复项时，保留全部行为约束和验收场景。
- 代码和自动化完成后，将实现记录移入 changelog。TODO 只保留剩余行为和验收动作。
- 修复世界数据损坏、不可恢复和状态锁死问题的优先级最高。

## 状态机与组件化重构计划（2026-09-19）

### 基线与行为依据

- 唯一实施目录：`D:/重要的资料/项目/__mc/fastformer`。覆盖客户端与服务端，不用 C 盘工作树覆盖代码。保留已有修改和未跟踪文件。
- 本次检查基线：`master`，HEAD 为 `bac71b8`，工作树存在大量未提交修改。实现前重新核对其他 worker 的范围和最新修改，不将 HEAD 当作完整运行版本。
- 交互以 `docs/principles/pseudocode/sessions` 为准。明确规则替换旧行为；`待定` 项记录 D 盘当前实际行为并补测试；伪代码内部冲突先确认。
- 只有选区操作会话，不另设工作区会话。原工作区的部件、选择集、变换和提交职责归选区会话。验收条目中的“工作区”仅指现有代码的数据容器，不代表新增会话。
- 按职责、重复决策和状态所有权整理代码，不按文件长度机械拆分。已有等价实现优先复用，不另建平行框架。
- 默认保持网络与磁盘格式兼容，不引入真正的 Minecraft 实体或数据包组件配置。
- 初次 `./gradlew test` 使用缓存。2026-09-19 再次执行 `./gradlew test --rerun-tasks` 成功，测试实际执行；选区草稿迁移后 `./gradlew test` 也实际执行并成功。结果不替代实机验收。
- 2026-09-19 用户确认当前版本为回退版本，原 bug 不存在，因此删除旧版本 bug 待办，不记为本轮修复。保留重构回归测试和通用验收要求，它们不代表当前存在故障。

### 核心架构任务

### 当前已完成子项（2026-09-19）

- [x] 选区操作模式 OperationMode、阶段模式 OperationStageMode 和重复范围 OperationStackRegion 归入 fastplace.selection，重复范围测试同步迁移。客户端、服务端、网络和草稿引用已更新，资源与脚本无旧类名依赖。完整 `test check` 及 `runGameTestServer` 通过，85 项必需 GameTest 全部通过，日志见 `.dsh-tmp/selection-transform-package-gametest.log`。

- [x] 2026-09-20 将 OperationSelectionMode、OperationSelectionStage、OperationSelectionVolume 及体积测试移入 fastplace.selection，统一客户端、服务端及协议引用。网络仍用枚举序号，草稿仍用枚举名称，枚举顺序和名称未变。完整 `test check runGameTestServer` 通过，85 项必需 GameTest 全部通过，日志见 `.dsh-tmp/selection-package-integration.log`。包归类不代表其余架构任务完成。

- [x] 2026-09-20 拆出选区快照采集、选区变换计算、Gizmo 绘制及共用四边形写入。Gizmo 显示与尺寸类归入 client.gizmo。完整 `test check runGameTestServer` 通过，85 项必需 GameTest 全部通过。随后新增控制器测试确认 Alt 完成后只选中新对象、旧对象可重新选择，混合提交成功清空全部对象。见 [规则与职责拆分记录](../changelog/2026-09-20-selection-rules-and-domain-split.md)。其他架构任务及实机未完成。

- [x] 已有选区普通中键按用户确认的规则入队：归属选区即拦截原版，无变化、无落点或失败也不让行。删除旧中键同步调整分支，左右中点编辑与对象点击共用路由判断。点编辑队列共 12 项测试通过，2026-09-19 19:14 完整 `test check` 通过，耗时 40 秒。伪代码和交互规范已同步，见 [中键入队记录](../changelog/2026-09-19-selection-middle-mailbox.md)。实机未验收，本轮未重跑 GameTest。

- [x] 已有选区普通左右键点编辑接入物理队列，固定 owner、活动部件实例和世界落点；删除后复用编号拒绝旧事件，同实例连续设点仍按顺序执行。新增七项测试通过，2026-09-19 19:02 完整 `test check` 通过，耗时 29 秒。见 [已有选区设点入队记录](../changelog/2026-09-19-selection-point-mailbox.md)。中键、旧手势兼容分支及实机仍待完成。本轮未重跑 GameTest。

勾选表示所述实现及自动化完成，不表示整个阶段完成，也不表示实机通过。2026-09-20 快速起形 Enter 数据来源修正后，完整 `test check` 与 87 项 GameTest 通过。之后路径闭合职责拆分通过完整 `test check`，未重跑 GameTest。各次验证的具体范围见对应 changelog。客户端实机验收仍未完成。

进度速览：选区数据归属、草稿阶段处理、键盘与滚轮队列、选区交互对象及部分历史恢复验证已完成。鼠标入队、完整会话生命周期、跨会话组件迁移、完整预览流水线、服务端生命周期统一和实机验收仍未完成。路径闭合拆分与时间快照见 [实现记录](../changelog/2026-09-20-path-close-input.md)。下面保留各项证据与范围，不用一个总勾选代表整轮重构完成。

本次进度核对：已完成的子项集中勾选在本节，五个阶段均保留“部分完成”。勾选对应各自记录中的验证范围，不表示当前整个工作树通过验证。

- [x] 普通选区 Gizmo 和面目标接入物理事件队列，统一为 `SelectionPointerEvent` 和 `SelectionPointerCapture`。新按下先取消旧捕获，释放绑定具体编辑与路由令牌。保留 8 项点击测试，新增 7 项拖动测试，15 项全部通过。2026-09-19 18:06 的完整 `test check` 通过，耗时 45 秒。面测试覆盖已接受捕获的结算，不替代真实世界准入和实机拖动验收，见 [拖动队列记录](../changelog/2026-09-19-selection-pointer-mailbox.md)。
- [x] Alt 直接命中 Gizmo 的左／右键拖动接入同一队列。按下固定物理 Alt 值，分派不重读修饰键，接受拖动后消耗 Alt 短按。新增 3 项测试，覆盖 Alt 松开顺序、拒绝事件不消耗修饰键及普通按下不继承后来的 Alt 状态。18 项队列测试全部通过，2026-09-19 18:22 完整 `test check` 通过，耗时 3 分 38 秒。伪代码和交互规范已记录用户确认的 Gizmo 例外。Alt 新建草稿随后已入队，见下一项；实机未验收。
- [x] 本地普通 `CreateSelection` 和 Alt 新建草稿接入同一队列，删除两处同步新建分支。固定点击坐标、按钮、修饰键、owner 和模式，连续点击按当前阶段顺序执行。新增 9 项设点队列测试及 1 项拖动到草稿的替换测试，两类队列测试共 28 项全部通过。2026-09-19 18:40 完整 `test check` 通过，耗时 3 分 7 秒。见 [草稿入队记录](../changelog/2026-09-19-selection-draft-mailbox.md)。服务端初始设点、已有选区点编辑及实机仍未完成。

- [x] 选区草稿由不可变快照和阶段处理器推进，撤销恢复点、范围和底面计数。七项新增测试及全量 `test check` 通过，见 [草稿状态机记录](../changelog/2026-09-19-selection-draft-state-machine.md)。
- [x] 选区会话建立 `IDLE / POINTING / FOCUSED / SUBMITTING` 生命周期 owner，区分环境退出与完整清理。三项新增测试及全量 `test check` 通过；顶层会话迁移仍未完成。
- [x] 提交 tracker 绑定选区 owner，控制器门禁与回执检查同一归属。定向测试通过，见 [提交 owner 边界记录](../changelog/2026-09-19-submission-owner-boundary.md)。
- [x] 标签实例身份不随数字编号复用。五项新增测试及全量 `test check` 通过，见 [标签实例身份迁移记录](../changelog/2026-09-19-label-instance-identity.md)。
- [x] 选区面与 Gizmo 拖动由会话持有互斥状态，更新和释放检查会话、手势及部件实例。相关入口与所有权测试通过；其他会话手势仍待迁移。
- [x] 选区标签、边框和 Gizmo 声明类型化点击动作，按下事件保存对象快照，hover 与点击共用目标解析。三项新增测试及全量 `test check` 通过，见 [点击动作组件记录](../changelog/2026-09-19-interaction-press-binding.md)。跨会话动作和 hover 进入/离开动作仍待完成。
- [x] Gizmo 渲染与命中共用可见性组件，隐藏手柄不能命中。取消选择后清理隐藏对象的 hover，标签仍可重新选择。修改一项命中测试并新增一项 hover 测试，完整 `test check` 通过，见 [可见性组件记录](../changelog/2026-09-19-gizmo-visibility-component.md)。保留当前显示规则，实机仍待验收。
- [x] 选区普通部件、标签和边框的左右键按下与释放进入键盘／滚轮共用队列，固定目标、Ctrl、时间及物理配对编号。新增 8 项生产分派测试，覆盖顺序、重叠按钮、迟到释放、源数据替换、取消、提交和恢复门禁。完整 `test check` 通过，见 [点击入队记录](../changelog/2026-09-19-selection-click-mailbox.md)。此记录属于普通点击迁移阶段，Gizmo 和面目标随后已入队，见拖动队列记录。Alt Gizmo 与本地草稿随后已入队，见上方子项。其余同步鼠标路径先收取更早的队列事件，尚未统一为 tick 收取。

- [x] 输入分派的 `OPERATION_ADJUST` 左键路径已去除重复的 cuboid/prism 三元决策：统一尝试 Gizmo，仅 prism 回退到点/面调整；定向输入测试通过。
- [x] 选区草稿拒绝事件和空坐标不推进生命周期，非法事件无副作用。
- [x] 键盘快照固定按键、扫描码、动作、修饰键和发生时间，并按顺序进入玩家会话 mailbox。
- [x] 候选滚轮事件进入同一物理事件队列，回调只决定原版拦截，tick 再分派候选更新。
- [x] 键盘与滚轮共用同一队列，保持跨类型发生顺序；会话 reset 丢弃旧队列事件。
- [x] 草稿恢复等待物理输入和提交回执队列处理结束，正在分派的批次也阻止恢复覆盖当前编辑。
- [x] 接受取消后丢弃旧物理输入，包括当前批次剩余事件；拒绝取消时保留队列。
- [x] 每个物理事件分派前检查玩家、连接、菜单和窗口焦点，失效时丢弃物理输入，但保留提交回执。
- [x] 上述队列、恢复门禁及取消行为已有 `ClientInputSessionKeyboardMailboxTest` 覆盖，修改后完整 `./gradlew test check` 通过。
- [x] 左右键共用 `finishMouseRelease`，移除重复释放分派，保留左键独有的撤销清理。新增两项入口测试覆盖按钮匹配、重复释放和左键 geometry 捕获；完整 `test check` 通过。拖动路径仍同步，普通选择点击随后已入队，见上方子项。
- [x] `SelectionPointerPress` 固定捕获时刻，选区面拖动读取该时间，不在动作执行时重新计时。快照测试已检查时间范围，完整 `test check` 通过。释放时间已在下一项完成，统一手势身份及完整鼠标入队仍待迁移。
- [x] 左右键释放在回调入口记录时间，撤销短按、操作面短按和控制点双击共用该时间，不再分别读取执行时刻。新增撤销释放时间注入测试，完整 `test check` 通过。捕获身份和鼠标入队仍未完成。
- [x] 网络回执、提交完成通知和异步结果在收取前校验当前 owner、环境及 revision/request 身份。
- [x] 选区数据、草稿阶段、提交 tracker 和输入所有权已集中到客户端会话边界，旧会话令牌不能修改当前编辑。
- [x] 选区标签、边框和 Gizmo 已发布稳定交互对象快照，并共享世界坐标几何、hover、提示和拖动捕获状态。
- [x] 交互对象已声明 `SELECTION_ROLE`：原始选区、变换部件、剪贴板部件；场景与标签测试已覆盖角色传递。
- [x] 源方块遮罩已接入区块编译和方块实体渲染：遮罩快照同时过滤方块、流体、区块内方块实体及旧区块列表中的方块实体，并在变更后使受影响区块失效。
- [x] 世界选区提交计划会排除未变换及已恢复的原始选区，剪贴板部件和待删除内容仍保留在提交计划中。
- [x] 选区/部件集合比较已采用 hash 快速筛选加完整集合核对，覆盖对称恢复和 hash 碰撞回归测试。
- [x] 已变换部件通过 `withBlocks` 恢复原集合时，复用变换恢复判断，立即恢复选区身份、面编辑能力及源显示。新增两项模型测试覆盖直接内容恢复和世界坐标等价后的存储归一化，完整 `test check` 通过。测试使用合成快照，不替代真实方块状态、方块实体及实机验收。
- [x] `SelectionRestorationGameTests` 补充三项真实数据测试：熔炉朝向差异、箱子库存差异与固定基线、对称石块旋转恢复。2026-09-19 16:07 完整 `test check runGameTestServer` 成功，84 项必需 GameTest 全部通过。该证据覆盖模型身份与能力，不替代客户端显示和操作验收。
- [x] 场景测试覆盖选区到部件再恢复的完整往返：标签、边框、Gizmo 角色同步，稳定 ID 和原范围保留，旧快照不变。完整 `test check` 通过。会话树索引与交互规范已移除旧 D3 规则；未新增未经确认的标签样式。
- [x] 场景与独立 Gizmo 创建共用 `SelectionRole.from`，删除重复的角色判断。完整 `test check` 通过。
- [x] 物理事件批次固定输入会话、玩家、连接和世界对象，每次分派前核对身份，防止批次中途环境替换后继续执行旧输入。完整 `test check` 通过；真实重连和换维度仍待实机验收。
- [x] 历史持久化失败时保留 dirty 和可恢复副本，未完成持久化时跳过缓存裁剪。
- [x] 历史持久化回调按写入 generation 收敛；较旧快照成功不得清除较新失败的 dirty 状态。
- [x] `HistoryPersistenceOrderingGameTests` 通过可控 Future 验证旧快照成功、普通写入成功均不清除较新失败，最新完整快照成功才恢复。2026-09-19 首轮集成中已有取消测试等待相邻批次写入锁超时；未改代码重跑 `runGameTestServer` 成功。该测试验证回调顺序，不代表真实磁盘故障验收。
- [x] 四项取消结算 GameTest 分为独立批次，避免同批争用维度写入锁。保留原超时与断言。2026-09-19 17:08 完整 `test check runGameTestServer` 成功，日志确认四个单测试批次分别执行，85 项必需测试全部通过。
- [x] 首写后恢复、历史封口及索引发布边界已通过现有双进程/GameTest 自动化证据。
- [x] 2026-09-19 17:08 完整 `./gradlew test check runGameTestServer` 已通过，85 项必需 GameTest 全部通过。证据为 `run/gameTestServer/logs/latest.log` 中 17:08:42 的完成记录。此记录不覆盖之后的 Gizmo 可见性修改，也不替代客户端实机验收。

当前输入边界剩余任务：

- [x] 选区控制点拖动的启动、更新、释放和短按／双击结算集中到 OperationPointInputController，显式传入输入会话。拖动快照保存捕获令牌，约束及位置变化保留身份，迟到更新和释放不结束新手势。新增三项生产入口回归测试，完整 `test check` 通过。主输入类减少约 170 行，控制点按下的完整快照入队仍未完成。
- [x] 控制点取消统一到控制器，四处输入清理入口同时释放自身捕获和双击记录。新增两项测试覆盖重复取消、迟到释放与替换捕获隔离，完整 `test check` 通过。持续拖动捕获规则已写入状态机伪代码；完整鼠标入队和实机验收仍未完成。
- [x] 操作面与操作 Gizmo 的启动、持续更新、释放和取消集中到 `OperationDragController`，主输入类仅保留工作区目标路由。拖动保存捕获令牌，所有状态复制保留令牌。新增四项生产入口测试覆盖同类型捕获替换、迟到释放、预览退出和重复取消，完整 `test check` 通过；启动职责迁移后再次通过完整检查。此项落实状态机的持续拖动捕获规则，按下快照入队和实机验收仍未完成。

- [x] 几何会话失效时，更新和释放入口同时清除拖动数据及其指针捕获。正常完成、会话失效和过期拖动共用按令牌清理，不结束替换后的新手势。新增两项生产入口回归测试，完整 `test check` 通过。此项不代表完整鼠标迁移或实机验收完成。

- [x] 选区滚轮移动进入键盘和鼠标共用物理队列，固定视线轴向偏移、选区 owner 和部件实例身份。滚轮分派拆入 ScrollInputDispatcher，连续移动保留顺序，旧 owner、编号复用、选择集变化及提交门禁拒绝旧事件。新增五项生产分派测试，完整 `test check` 通过。同步鼠标按下和兼容收取边界仍保留，完整鼠标迁移及实机未完成。

- [x] 旧操作和特殊形状的已捕获鼠标释放进入共用物理队列，保存按钮、物理释放时间、点击令牌、指针令牌和结算目标。分派拒绝被替换的捕获与目标，重复释放最多结算一次。六项新增测试覆盖队列顺序、时间、重复释放、替换、取消及环境失效，完整 `test check` 通过。尚未捕获的释放、同步按下和兼容队列收取仍待迁移，不代表完整鼠标入队完成。
- [x] 释放入队入口同时核对手势令牌和捕获按钮。按钮不匹配时保留同步兼容路径，不提前拦截该释放。新增按钮配对回归测试，完整 `test check` 通过。

- [x] 修正特殊形状撤回用世界原点作为占位命中的错误。普通撤回删除最后点，明确命中删除仍可删除原点。两项新测试覆盖含原点草稿的连续撤回和明确删除，完整 test check 通过。各形状阶段 D5 仍待核对。

- [x] 几何拖动生产入口先拒绝失效捕获，再读取预览和世界。新增两项入口测试直接调用更新与释放，确认旧拖动不能结束同类型或其他类型的新手势，重复释放不访问世界。完整 test check 通过。

- [x] 几何拖动快照保存捕获令牌，步数更新保留令牌，释放只结束自己的手势。回归测试以会话令牌已被新手势替换的状态验证旧拖动不发送，完整 test check 通过。鼠标释放事件本身的快照入队仍未完成。

- [x] 几何拖动更新与释放按捕获的手势类型选择协议，不因选区就绪状态变化而切换发送目标。无效令牌或所属会话退出后不发送。三项新测试覆盖目标隔离、令牌替换与取消，完整 test check 通过。物理按下与释放快照的完整身份迁移仍未完成。

- [x] 特殊形状点击、设点、右键优先级和 Gizmo 启动拆入 GeometryInputController，显式接收输入会话。几何拖动更新与释放拆入 GeometryDragController，接收输入会话和 Ctrl 状态。两轮完整 test check 通过。完整鼠标入队及拖动捕获身份迁移仍未完成。

快速起形模式和控制点目录归类已完成，具体范围与验证见 [职责归类记录](../changelog/2026-09-20-quickshape-and-controlpoint-packages.md)。它不代表下列会话与队列迁移完成。

- [x] 鼠标按钮、按下路由和拖动释放规则归入 `client.input.mouse`，三组对应测试同步归类。调用入口显式导入鼠标规则，完整 `test check` 通过。本次只调整归属，不改变按钮语义，也不代表鼠标队列迁移完成。
- [x] 鼠标释放的目标选择与业务结算拆入 `MouseReleaseDispatcher`，显式接收输入会话。队列分派沿用已校验的会话，控制点、Gizmo、选区面和撤销短按共用此入口。按钮和物理时间测试直接调用分派器，新增会话隔离测试。完整 `test check runGameTestServer` 通过，91 项游戏测试全部通过。同步兼容路径仍待迁移。
- [x] `PhysicalInputMailbox` 统一持有物理输入事件、待处理设点计数和队列失效释放，`ClientInputSession` 保留交互状态与取消策略。既有批次顺序、批次内取消、环境失效及设点清理测试随完整 `test check` 通过。此项为队列职责拆分，剩余同步输入尚未入队。

快速起形逐点回退、多边面中键无操作见 [阶段输入记录](../changelog/2026-09-20-quickshape-stage-input.md)。提交等待已有实现与自动化，范围见 [提交等待记录](../changelog/2026-09-20-quickshape-wait.md)。客户端实机仍未验收。

- [x] 菜单接管和会话退出共用 SelectionGestureController.cancelActive 回退选区编辑，按捕获令牌释放指针。完整 `test check` 通过，新增回归测试确认回退内容、重复取消无副作用、迟到释放不提交旧编辑。定向测试通过，日志见 `.dsh-tmp/selection-context-cancel-test.log`。真实菜单与失焦场景仍待实机验收。

- [x] 旋转输入补充三个行为测试：Alt 只消耗所属会话、Ctrl 优先且不消耗 Alt 短按、普通旋转不激活修饰键。选区按下测试直接调用 SelectionGestureController，删除旧私有入口及两处反射。完整 `test check` 通过，日志见 `.dsh-tmp/selection-entry-cleanup.log`。

- [x] 选区拖动逐帧更新迁入 SelectionGestureController，与启动和结算共用显式输入会话。旋转精度规则提取为 RotationInputAngles，选区更新不再通过旧入口读取全局修饰键状态。完整 `test check` 通过，日志见 `.dsh-tmp/selection-rotation-owner.log`。这项拆分不代表快速起形、特殊形状及完整物理输入迁移完成。

- [x] 选区面与 Gizmo 的拖动启动、提交结算和令牌释放归入 SelectionGestureController。SelectionInputDispatcher 显式传递批次输入会话，不再调用大型输入类执行选区按下或结算。旧同步入口暂时转发，拖动逐帧更新仍待迁移。完整 `test check` 通过，日志见 `.dsh-tmp/selection-gesture-lifecycle.log`。

- [x] 选区事件分派从 FastPlaceClientInput 拆入 SelectionInputDispatcher，集中处理草稿、点编辑、按下、释放和取消。分派显式接收当前队列批次的输入会话。三组队列测试删除反射，直接检查生产分派入口。完整 `test check` 通过，日志见 `.dsh-tmp/selection-dispatch-owner.log`。底层拖动入口和其他会话迁移仍未完成。

- [x] 服务端初始设点请求进入同一物理队列，保存 owner、模式和服务端选区身份。发送校验集中到 RemoteSelectionPointDispatcher，请求快照不读全局控制器。队列测试覆盖顺序、取消、环境失效、提交门禁与本地草稿冲突，新增身份测试覆盖模式改变、服务端选区替换和清除。完整 `test check` 通过，日志见 `.dsh-tmp/remote-point-final-check.log`。实机未验收，其他同步鼠标路径仍需迁移。

- [x] 每个物理事件分派前检查上下文，测试覆盖批次中途上下文失效、剩余键盘与滚轮事件不再重放，以及恢复后接受新输入。菜单实机操作仍待验收。
- [ ] 完成剩余鼠标按下、释放及业务 hover 入队，保留按钮和手势身份。普通 Part、Gizmo、面目标、Alt Gizmo、本地 CreateSelection 和 Alt 新建草稿已入队。已有选区普通左右中键点编辑也已入队。服务端初始设点已入队，剩余包括旧手势兼容分支、快速起形及特殊形状。最终移除同步鼠标路径前的队列收取兼容边界。
- [x] 2026-09-20 用户确认两条设点规则：Alt 新建保留旧选择集，仅转移输入焦点；普通 CUBOID 空草稿中键不设点，等待左键。控制器、客户端草稿状态机及服务端补点入口已修改，新增三项回归测试。完整 `test check runGameTestServer` 通过，85 项必需 GameTest 全部通过，日志见 `.dsh-tmp/selection-rule-validation.log`。实机未验收。

- [ ] **D3 选区与部件转换（2026-09-19 用户确认）。** 设点和范围编辑不生成部件。拖动、stack、旋转等变换改变方块集合后才成为部件。后续变换恢复原始方块集合时，部件变回选区。比较世界位置、方块状态与方块实体数据，不依赖变换参数或肉眼判断。hash 相同时核对完整集合。原始集合固定，不从后续变化的世界重读。同步修改控制器、显示、编辑能力、提交与恢复入口，并验证对称结构旋转、逆变换、方块状态和方块实体差异。
- [ ] **状态机与唯一写入者。** 会话采用 `enter / onEvent / tick / exit`。当前状态判断退出原因，状态机统一切换；任意时刻只有一个活动会话，同一 tick 可以顺序切换多次。阶段、手势和提交过程分别建模，由同一客户端会话 owner 管理。选区数据归属已收拢，剩余生命周期与写入入口迁移仍需完成。
- [ ] **事件与原版拦截。** 输入回调转换为语义事件。必须即时判断的原版拦截只读已发布的输入归属，业务事件进入客户端线程队列，每个事件只处理一次。网络、后台计算和磁盘完成通知同样入队，不直接修改会话。
- [ ] **异步身份与渲染边界。** 事件携带环境、会话、revision 或请求身份。不匹配的结果丢弃并释放资源。渲染只读完整显示快照，不修改业务状态。`exit` 清理阶段资源，不递归切换，不默认删除全部草稿。
- [ ] **客户端优先。** 客户端承担输入、命中、选区编辑、形状生成、变换、预览和即时校验。服务端保留权限、限制、冲突检查、权威源数据、世界写入与恢复。服务端验证客户端结果，世界来源移动、复制及方块实体数据不得由客户端声明替代。
- [ ] **候选与显示分离。** 默认完整计算有效变化，计算期间保留上一份完整显示。仅在实测拖慢 tick 后合并中间请求。旧显示快照不得作为新候选提交。
- [ ] **固定提交意图。** 提交数据遵循会话伪代码。快速起形 Enter 默认丢弃预备点，只提交已确认部分；专门按键规则优先，目前唯一例外是滚轮模式保留预备点。基于选定数据创建唯一提交意图，计算与校验完成后自动提交一次。等待期间禁止形状修改，重复 Enter 不创建第二次提交。Q 取消未发送意图后，迟到结果不得发送；已发送请求等待服务端取消或恢复结果。校验失败不发送，新的提交需要新的 Enter。
  服务端数据来源修正与验证见 [Enter 实现记录](../changelog/2026-09-20-quick-shape-enter.md)。客户端已捕获完整快照的按键身份，服务端拒绝过期确认，见 [提交快照记录](../changelog/2026-09-20-quickshape-submission-snapshot.md)。完整快照的后台生成、一次提交与 Q 取消已有实现，见 [提交等待记录](../changelog/2026-09-20-quickshape-wait.md)。分片尚未齐全时的按键意图、本地候选和服务端验证客户端几何的完整路径仍未完成。
- [ ] **类型化交互对象。** `InteractionObject` 具有稳定 ID、所属会话及类型化组件集合，用于 Gizmo、边框、控制点、标签和提示，不为每个预览方块创建对象。首批组件包含位置与锚点、可见性、显示内容、命中几何、高亮样式、悬浮提示、点击与拖动动作绑定。
- [ ] **统一组件系统。** 统一执行命中、hover 进入与离开、高亮、提示和渲染。显示和命中共享世界坐标几何，相机偏移只在渲染边界应用。`on_hover` 等类型化动作产生语义事件，不持有直接修改业务状态的任意回调；选中状态读取会话，不复制权威状态。
- [ ] **捕获与销毁。** 拖动捕获对象 ID、按钮和手势身份；对象销毁、会话退出和环境变化时释放捕获与 hover。视觉反馈可随帧更新，业务修改仍经过事件队列。
- [ ] **服务端任务生命周期。** 保留现有生成、写入、journal 和恢复实现，通过组合统一生成、快照、准备、写入、提交、恢复与终态。预约、journal 和恢复责任只有一个持有者，交接与最终释放最多执行一次。服务端线程统一修改任务状态和世界，后台生成、压缩和磁盘任务只返回带身份的结果。
- [ ] **历史所有权。** 集中管理每个玩家的活动历史状态。调度、持久化、恢复和缓存不各自保存 pending 或锁状态。安全持久化前保留唯一可恢复副本，活动任务和恢复数据不可淘汰。历史发布故障测试通过后，再调整缓存淘汰与重启加载路径。

内部边界：`ClientSemanticEvent`、`InteractionTransition`、`SubmissionIntent`、`PreviewRevision`、`PreviewSnapshot`、`InteractionObject`、`ComponentType<T>`、`TaskOutcome` 与集中管理的历史 owner 状态。已有等价接口优先复用。

2026-09-19 早期增量记录：键盘快照与 D3 集合比较先完成基础测试。此后键盘与滚轮已入队，D3 已补提交过滤、场景角色及真实方块状态和方块实体 GameTest，具体完成范围见上方勾选。空数据和超预算结果尚不能证明集合相同。2026-09-20 用户确认混合提交成功后清除全部选区和部件，现有成功回执路径调用 clearWorkspace，符合此规则。客户端显示和操作仍待实机验收，因此 D3 总项保留未勾选。

### 分阶段交付

D3 提交入口增量：未变换及已恢复的世界选区从提交计划排除，复制仍可使用这些选区。剪贴板部件和待删除内容继续提交。提交来源按过滤后的实际计划判断。`ClientSelectionPart.isOriginalSelection()` 现在显式区分原始选区和已变换部件。新增三项内容准备测试，2026-09-19 完整 `test check` 实际执行通过。随后 `./gradlew runGameTestServer` 成功，服务端 GameTest 未发现回归。混合提交成功清空全部选区和部件的规则及控制器测试已完成；显示入口及实机仍待验收。

交互对象现在发布 `SELECTION_ROLE` 组件，明确标记原始选区、变换部件和剪贴板部件。标签、边框和 Gizmo 共用该角色快照，标签展示也读取并携带角色。新增场景测试，定向 `PartLabelInteractionTest` 与 `SelectionInteractionSceneTest` 通过。随后完整 `./gradlew test check` 实际执行通过。完整渲染动作系统仍需消费该组件，实机验收未完成。

| 阶段 | 交付内容 | 已实现 | 自动化通过 | 实机通过 |
| --- | --- | --- | --- | --- |
| 1. 建立基线 | 记录分支、改动和运行版本；核对 worker 范围；建立事件转换表及待定行为清单；记录测试基线 | 部分完成，选区草稿矩阵已记录，其他入口待补 | 全量基线测试实际执行通过 | 未执行 |
| 2. 客户端状态 | 先迁移选区，再迁移快速起形和其他会话；统一输入、阶段、提交和取消；旧入口仅转发，状态只有一个写入者 | 部分完成，选区数据归属收拢，网络回执和提交完成事件进入 mailbox；键盘与候选滚轮共用有序队列，已补草稿恢复门禁、取消及逐事件上下文检查；普通 Part、Gizmo、面目标、Alt Gizmo、本地 CreateSelection、Alt 新建草稿和已有选区左右中键点编辑已入队；服务端初始设点已入队；旧手势兼容路径、快速起形、特殊形状和完整会话生命周期待迁移 | 最近一轮修改后完整 test check 通过；具体证据见完成子项和迁移记录 | 未执行 |
| 3. 交互对象 | 先迁移标签和 hover，再迁移边框、控制点和 Gizmo；保留方块批量渲染，逐类替换旧命中与显示 | 部分完成，选区标签、边框及两类 Gizmo 已发布对象快照，hover、拖动状态归会话所有，提示策略已组件化；控制点、动作绑定及跨会话迁移待完成 | 组件、捕获、hover、提示及场景测试通过，全量 test check 实际执行通过；具体证据见迁移记录 | 未执行 |
| 4. 预览异步边界 | 版本校验、完整快照交换和资源释放；核对并接收其他 worker 的源遮罩与剔面成果，不重复覆盖 | 部分完成，已有身份校验及源遮罩接入；完整显示快照流水线、跨层剔面生产路径仍待核对 | 已有局部测试，不代表整个阶段通过 | 未执行 |
| 5. 服务端与历史 | 先统一任务生命周期，再收拢历史 owner 与发布流程；删除旧状态写入、重复分支及无调用转发入口 | 部分完成，历史 dirty、持久化 generation 及回调顺序已有实现；任务生命周期统一仍未完成 | 局部恢复与发布边界测试通过；Enter 修正后的 87 项 GameTest 通过，不代表故障场景全部覆盖 | 未执行 |

每阶段形成独立可回退交付。逐项更新实现、自动化和实机状态，未通过对应验收不得将 bug 标为解决。

选区草稿的事件矩阵、待定行为和迁移证据见 [选区阶段迁移记录](selection_transition_baseline.md)。Terra 审计调用因服务不支持该模型而失败，本次由主代理执行，未产生子代理修改。

输入路由的当前归属、语义事件转换和队列边界见 [输入路由迁移基线](input_routing_baseline.md)。物理按钮与会话阶段的完整矩阵仍待补齐，该记录不是实现或测试完成证据。

成功回执的观察状态已在事件入队前校验，避免非法事件在收取时丢弃后续合法回执，见 [提交事件校验记录](../changelog/2026-09-19-submission-event-validation.md)。新增两项测试，完整 `test check` 通过。物理输入队列仍待迁移。

选区草稿拒绝事件和空坐标不再推进会话生命周期。新增两项测试，覆盖各生命周期、选区模式及合法事件进入设点阶段。2026-09-19 完整 `test check` 实际执行通过。物理事件队列、完整会话迁移和实机验收仍未完成。

网络队列、选区所有权与测试范围见 [2026-09-19 迁移记录](../changelog/2026-09-19-selection-owner-and-mailbox.md)。后续交互迁移后的集成结果见 [集成回归记录](../changelog/2026-09-19-integration-regression.md)：2026-09-19 05:20 的 `runGameTestServer` 成功，81 项必需测试全部通过。该结果不替代客户端实机验收，也不覆盖之后的修改。

输入路由的类型化事件、只读检查及统一切换见 [输入事件迁移记录](../changelog/2026-09-19-input-transition-events.md)。这不是全部会话生命周期的完成记录。

输入路由、提交门禁、指针、拖动、修饰键和按键历史现归玩家会话所有，见 [输入所有权记录](../changelog/2026-09-19-input-session-owner.md)。作用域切换清理瞬时输入，草稿恢复不恢复提交等待。键盘业务事件现按发生顺序进入玩家会话 mailbox，在 tick 边界分派；Q/Esc 保留回调内即时取消以拦截原版动作。候选滚轮事件也进入同一会话 mailbox，回调只决定拦截，tick 再发送候选更新。键盘和滚轮现在共享一个物理事件队列，保持跨类型顺序。新增 mailbox 测试，最新完整 `./gradlew test check` 实际执行通过。鼠标按下/释放和 hover 事件仍待统一入队。

标签的组件、共享几何、行为变化及剩余边界见 [标签组件迁移记录](../changelog/2026-09-19-label-interaction-components.md)。旧文字规则保留，悬浮上下漂移取消，实机效果尚未验收。

标签实例身份见 [标签实例身份迁移记录](../changelog/2026-09-19-label-instance-identity.md)。数字编号复用不再复用对象身份；新增五项测试，全量 `test check` 通过。

标签与边界快照见 [选区交互场景迁移记录](../changelog/2026-09-19-selection-interaction-scene.md)。新增八项测试，全量 `test check` 通过。hover、拖动捕获、完整预览快照与实机验收仍未完成。

标签视觉悬浮身份与清理见 [悬浮生命周期记录](../changelog/2026-09-19-label-hover-lifecycle.md)。新增四项测试，全量 `test check` 通过。类型化事件尚未接入业务分发，动作绑定、捕获及提示组件仍待迁移。选区 D3 完成入口冲突已请求确认，未自行更改。

编辑归属隔离见 [编辑令牌隔离记录](../changelog/2026-09-19-edit-token-isolation.md)。新增三项测试，全量 `test check` 通过。旧会话令牌不得操作当前编辑；统一对象捕获与实机验收仍未完成。

空手势与迟到释放见 [手势守卫记录](../changelog/2026-09-19-pointer-empty-state.md)。新增三项测试，全量 `test check` 通过。物理输入队列和统一对象捕获仍未完成。

边框对象与共享世界坐标组件见 [边框迁移记录](../changelog/2026-09-19-frame-interaction-object.md)。新增三项测试，全量 `test check` 实际执行通过。边框样式、动作绑定、统一捕获和实机验收仍未完成。

面与 Gizmo 拖动的目标实例捕获见 [拖动捕获记录](../changelog/2026-09-19-selection-drag-capture.md)。新增三项测试，全量 `test check` 实际执行通过。更新和释放校验会话、手势及部件实例，统一会话捕获管理与实机验收仍待完成。

拖动释放补充两项输入入口测试：迟到令牌和空选区释放不得结束新手势。释放改用捕获令牌，全量 `test check` 实际执行通过。证据见同一拖动捕获记录。

选区面与 Gizmo 拖动已从输入类的静态字段迁入会话持有的互斥状态，见 [手势所有权记录](../changelog/2026-09-19-selection-gesture-owner.md)。新增三项测试，全量 `test check` 实际执行通过。物理事件队列、全局指针令牌和其他会话手势仍待迁移。

选区按下事件固定目标、按钮、Ctrl 和短按方向，见 [按下事件记录](../changelog/2026-09-19-selection-pointer-press.md)。新增两项测试，全量 `test check` 实际执行通过。当前仍同步处理，环境身份、完整修饰键及释放事件尚未统一入队。

按下事件进一步固定会话 owner、部件实例及源数据版本，公共 Gizmo 固定选择集。新增三项测试，包含拒绝旧事件无副作用的输入入口验证，全量 `test check` 实际执行通过。完整连接环境校验和物理输入队列仍未完成。

单部件 Gizmo 由选区场景发布坐标框架组件，渲染与命中共享几何。公共 Gizmo 的范围及手柄生成已去重，见 [Gizmo 迁移记录](../changelog/2026-09-19-gizmo-geometry-components.md)。新增两项测试并迁移原轴向测试，全量 `test check` 实际执行通过。句柄动作绑定及实机验收仍待完成。

公共 Gizmo 现已作为会话级对象发布，渲染与命中不再自行读取实时选择集合并范围，见 [公共 Gizmo 场景记录](../changelog/2026-09-19-group-gizmo-scene.md)。选择输入立即发布新场景，新增一项控制器测试并迁移相关覆盖，全量 `test check` 实际执行通过。剩余为统一 hover、动作绑定、物理事件队列与实机验收。

选区标签、边框和两类 Gizmo 的 hover 已归同一会话状态，见 [对象 hover 记录](../changelog/2026-09-19-selection-object-hover.md)。对象或几何失效后清除旧命中，下一帧重采样。新增三项测试并补充命中来源断言，全量 `test check` 实际执行通过。剩余为跨会话 hover、提示组件、进入/离开动作分发及实机验收。

选区交互对象已携带类型化提示策略，旧提示接口转发到同一规则，见 [提示组件记录](../changelog/2026-09-19-interaction-tooltip-component.md)。新增三项测试，全量 `test check` 实际执行通过。旧入口退出、跨会话提示、动作分发及实机验收仍待完成。

选区标签、边框和 Gizmo 已声明点击动作，按下事件保存对象快照，输入入口执行类型化动作。hover 和点击共用目标解析，见 [点击动作组件记录](../changelog/2026-09-19-interaction-press-binding.md)。新增三项测试，全量 `test check` 实际执行通过。统一事件队列、跨会话动作、hover 进入与离开动作，以及客户端实机验收仍未完成。

### 测试与验收

选区草稿现由不可变快照和阶段处理器推进，撤销一次恢复点、范围及底面计数，见 [草稿状态机记录](../changelog/2026-09-19-selection-draft-state-machine.md)。新增七项测试，全量 `test check` 实际执行通过。2026-09-19 06:35 的 GameTest 全部 81 项必需测试通过。D3 行为未更改，顶层会话生命周期和物理输入队列仍未完成。

选区会话已增加 `IDLE / POINTING / FOCUSED / SUBMITTING` 生命周期 owner，环境退出和完整清理分开，见 [选区会话生命周期记录](../changelog/2026-09-19-selection-session-lifecycle.md)。新增三项测试，全量 `test check` 实际执行通过。控制器提交跟踪器、物理事件队列和实机验收仍未完成。

提交 tracker 已绑定选区交互 owner，回执和控制器提交门禁均需匹配当前 owner；空草稿事件在生命周期推进前拒绝，见 [提交 owner 边界记录](../changelog/2026-09-19-submission-owner-boundary.md)。定向 tracker/controller 测试通过；跨环境完整清理、物理事件队列和实机验收仍未完成。

历史持久化失败状态已与普通批次成功分离，只有完整快照修复成功才清除 dirty；未完成持久化时跳过缓存裁剪，见 [历史持久化失败状态记录](../changelog/2026-09-19-history-persistence-dirty-state.md)。历史 owner 测试和 `runGameTestServer` 已通过；真实磁盘故障和进程中断仍需实机验收。

提交完成回执已进入玩家输入会话 mailbox，在客户端 tick 边界推进 routing，见 [输入完成回执 mailbox 记录](../changelog/2026-09-19-input-completion-mailbox.md)。物理按键、滚轮和 hover 事件仍未全部入队，客户端实机验收仍未完成。

- [ ] 覆盖伪代码事件转换、非法事件无副作用、左右键身份、Q 拦截和原版让行。
- [ ] 覆盖同 tick Enter 与计算完成、重复确认、取消后迟到结果、重复回执、断线和换维度。
- [ ] 覆盖组件显示与命中几何一致、hover 切换、对象移除、拖动捕获及锁定能力。
- [ ] 覆盖移动源目标重叠、作物无支撑、方块实体、外部修改及失败后的状态一致性。
- [ ] 覆盖各类服务端任务取消、恢复交接、磁盘失败、进程中断、历史发布及 undo/redo 顺序。
- [ ] 每阶段运行相关测试和 `./gradlew test`；集成运行 `./gradlew check` 与 `./gradlew runGameTestServer`。需要重新执行的测试不得仅用 `UP-TO-DATE` 作为新增行为证据。
- [ ] 按 `runtime_acceptance.md` 留存客户端证据，记录完整预览、旧快照停留、tick/帧耗时及首块延迟。自动化不替代实机和故障恢复验收。

## 生命周期与性能验收

- [ ] 用相同结构采样生成、快照、journal、提交各阶段的峰值堆和对象驻留，确认数据按所有权边界释放。
- [ ] 测量小型操作的首块等待和剩余批次写入时间，区分快照、journal 与每 tick 写入预算。
- [ ] 以当前回退版本为基线，对普通放置及选区操作分别于生成、首写、后续写入及提交期间退出存档再进入。检查完整回退或完整提交、旧会话清理和后续操作。
- [ ] 在实际服务器运行三类任务时测试断线重连、换维度和世界卸载/重载。任务必须按策略等待或恢复，且不得丢失预约、锁死写入或恢复旧瞬时状态。
- [ ] 用备份测试存档在多段写入和封口提交边界结束服务器进程再启动。检查方块实体、流体、外部修改和恢复日志清理。
  - 首写后的未封口 journal 已通过独立进程重启测试，见 [首写后恢复证据](../changelog/2026-09-19-recovery-restart-audit.md)。多段写入、封口及上述复杂数据场景仍未验收。
- [ ] 测量增量预览的帧分配、连续移动帧时间、取消释放和显存释放。检查远坐标、呼吸透明度、遮挡、相机移动、透明排序及重复取消。

## 会话恢复

- [ ] **会话恢复实机验收。** 覆盖同进程重连、关闭客户端后重开、取消和换维度。按服务器/存档、玩家和维度隔离草稿；仅在服务器仍返回同一选择身份且玩家按 Enter 后恢复。按 Q、服务器无活动选择、选择身份变化、工作区引用失效或对应操作完成时必须清理草稿，且不得重复提交。检查 `min/max`、形状参数、部件、变换、源引用和方块实体；鼠标按键、Alt、拖拽、预览 Future、锁定、撤销历史和提交等待不得恢复。检查预览 revision、提交 request ID 和输入 generation，迟到结果不得改变新会话。服务端已写入任务继续由恢复日志处理。

## 历史与故障恢复

世界撤回/重做历史由服务端保存和校验。客户端草稿不是世界历史的权威来源。

- [ ] **历史协议真实中断验收。** 用备份存档在 batch 发布前后、journal 封口后、index 发布前后分别结束服务器进程并重启。确认启动 reconciliation 只恢复完整提交，保留可恢复 journal，并在索引修复后清理；不得启用尚未经过该验收的重启后旧历史操作。
  - 单操作的 batch 发布前、发布后未封口、封口后 index 发布前、index 发布后四个边界均通过双进程测试，见 [发布边界重启证据](../changelog/2026-09-19-history-seal-restart.md)。多操作顺序、复杂数据及三类任务完整调度仍待验证。
  - 同一组四个边界已补充一条既有历史，验证回退保留旧历史、提交恢复后新旧顺序正确。多个待恢复 journal 的排序和实际 undo/redo 仍待验证。
  - 两份已封口、未入索引且修改同一位置的 journal 已通过重启验证，世界重放与三条历史顺序正确。混合封口状态、恢复中再次中断及实际 undo/redo 仍待验证。
  - 同位置先封口、后未封口的混合场景已通过重启验证，后一次修改回退，前一次提交及旧历史保留。恢复中再次中断、复杂数据与实际 undo/redo 仍待验证。
- [ ] **旧历史按需加载实机验收。** 创建超过近期内存预算的撤回/重做历史，确认请求时逐页加载且不会卡住服务器线程。页面完成后重新检查维度、操作权限、世界写锁和方块冲突。覆盖加载期间禁用 FastFormer、换维度、断线、外部修改、损坏批次和重试；不得改变完整 undo/redo 顺序或丢失磁盘尾部。
- [ ] 覆盖磁盘满、文件损坏、版本不兼容、保存失败和真实进程中断。保存失败时保留可恢复数据，并显示准确原因，不能误报 JVM 内存不足。
- [ ] 补充多 UUID 上下线、超大单条历史、缓存淘汰再加载、undo/redo 顺序、新操作清空 redo、各保存阶段断电、损坏和版本迁移测试。
- [ ] 实机比较小结构首块、近期撤回和磁盘历史撤回延迟。重连和服务器重启后检查历史、方块实体和后台落盘卡顿。

## 架构与维护

- [ ] **输入分派验收。** Alt 新建选区、普通中键确认和 Q 取消已有纯语义矩阵测试。实机覆盖左右键、短按、释放、中键、Alt、取消和原版让行。提取公共路径不得改变按钮语义。
- [ ] **预览缓存验收。** 覆盖取消、换世界和重连。building、fallback、at-limit、GPU 和异步结果不得泄漏到新会话。
- [ ] **prism 草稿验收。** 覆盖普通/Alt、中键、撤销、闭合和提交边界。
- [ ] **PlacementTask 所有权。** 明确生成、快照、journal、写入、历史发布和恢复交接的数据所有者及释放条件。覆盖失败、取消、磁盘故障、进程中断和迟到异步完成。

## 交互架构

- [ ] **回调门禁实机验收。** building、geometry、activity、placement ack 和 operation 回调已检查连接、玩家、维度、服务器会话及各自 revision/request ID。实机覆盖重连后的旧连接回调、同连接换维度的旧回调、乱序快照和当前会话 placement ack。
