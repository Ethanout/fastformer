# 当前 TODO

本文件只记录尚未验证、且会影响玩家体验或数据安全的事项。模组以小型结构操作为主，首个方块的落下延迟优先于超大操作的总吞吐。代码结构调整必须服务于这些结果。
当前任务完成后，检查 `docs\plan\feature_todo\2026-08-31-anti-failure.md`，继续更新任务，并将已完成的部分写入更新日志 `docs\update`。

## 体验验收

- [x] 普通放置、工作区放置和选区操作在生成、压缩、写入、恢复各阶段显示正确状态。（已修复空生成结果错误进入空事务并显示失败的问题；完整阶段状态仍待客户端验收）
- [x] 一次 Q 输入取消本次操作，已写入内容自动恢复，恢复结束后回到正常状态；恢复中重复按 Q 不暂停恢复、不新增撤回任务。未创建恢复任务时不显示“正在回退”。需覆盖服务端任务终态与客户端状态同步，不能仅以构建通过验收。
- [x] 放置完成后立即撤回、写入冲突、邻居更新失败和恢复失败均显示可理解的结果。
- [x] 换维度或退出游戏结束当前交互会话，清除旧选区与预览；已写入内容的恢复必须继续完成。重连不能恢复旧交互状态，也不能丢失恢复责任。客户端断线 revision 已重置，服务端退出游戏路径和实机验收仍需检查。切屏不等同于退出游戏。
- [x] 删除 HUD 和帮助中的 Esc/Q 暂停提示：Q 是唯一退出键，不提供暂停操作；恢复期间显示恢复进度，完成后清除忙碌状态。


## 性能与资源

- [ ] 测量点击到首个方块、总耗时、单批 P95 和峰值堆内存。小型结构先按首次落块延迟排序。
- [ ] 单独测量小型操作的“首块等待”和“剩余批次写入”时间，区分快照/journal 等待与每 tick 写入预算。（日志已增加 `afterFirstWriteMs`，仍需相同场景实机采样）
- [x] 单独优化首块延迟：记录从确认输入到任务入队、生成完成、首个 expected 快照、journal 就绪和首个世界写入的时间点。
- [x] 找出首块路径中可以并行或提前完成的工作。不得跳过首块所需的冲突检查、恢复日志和世界写入许可。（已明确生成、首批快照、分段 WAL 准备可提前；冲突检查、恢复日志和 lease 仍是写入门槛）
- [ ] 单独设计并验证首块等待优化：让首批 before 快照与 journal 准备尽早开始，在首批 journal 就绪后立即写入；记录并比较“确认输入到首块”和“首块到完成”的两段延迟。（已避免已完成 journal future 的额外轮询 tick；首批分段 WAL 和客户端数据仍待验证）
- [ ] 首块延迟专项子任务：把确认输入后的等待拆成入队、生成、首批快照、journal、写入许可五段，分别记录耗时和线程/调度归属。（普通放置、工作区、选区及历史任务已有各阶段时间和线程名指标；`.233` 实机采样仍待补齐）
- [ ] 首块延迟专项子任务：验证小结构（1、16、256、4096、32768 方块）在单次操作中是否能在一个或极少 tick 内落下首批；不得用提高全局预算掩盖前置等待。
- [ ] 首块延迟专项子任务：以“首块可见时间”为主要门槛，比较优化前后中位数与 P95；总耗时仅作为次要指标，避免为吞吐牺牲交互反馈。
- [ ] 首块延迟实现专项：检查确认事件、任务入队、首批 journal 完成与主线程写入之间的 tick 边界；在安全条件满足后立即提交首批写入，避免仅因调度切换额外等待一个 tick，并用 1、16、256 方块场景验证。
- [x] 为首块设置独立验收目标。小型结构不能因为历史压缩、完整 after 快照或 undo 发布而等待首个方块。（代码路径已先完成首批世界写入，再收集 after 并发布历史；仍需游戏内测量具体延迟）
- [x] 评估并接入小型操作快速通道：在低内存压力、目标数量低于阈值时，提高单 tick 写入预算，目标是一次或极少 tick 完成。高风险方块实体和游戏内效果仍需验收。
- [x] 快速通道必须保留冲突检查、恢复日志和世界写入许可。超过耗时上限时自动退回普通分批模式。（路径仍经过完整快照/journal/lease；预算测试覆盖压力和阈值回退）
- [x] 将“世界写入”和“撤回历史”作为两个阶段设计。写入阶段只等待必要的冲突快照和 write-ahead 恢复日志，首批方块不等待完整 after 快照或历史压缩。
- [x] 将恢复日志与可撤回历史分成两个明确的所有权对象。恢复日志服务于崩溃和中途失败，历史批次服务于完成后的 undo/redo。
- [x] 保留一次操作的 operation ID 和 before/after 对齐规则。写入完成后再异步构建压缩历史，历史构建失败不能把已完成的世界写入报告为未写入。
- [ ] 用相同场景比较生成、快照、压缩和世界写入的等待时间。
- [ ] 确认 `32768`、`131072`、`262144` 和超过 `262144` 方块的放置与撤回行为。
- [ ] 内存不足时优先等待或减小批次。只有无法保留安全工作集时才拒绝操作。
- [ ] 验证生成结果、快照、journal 和历史压缩不会无故重复保留完整数据。（`WorldChangeBatchTest` 已验证 dense/uniform 历史批次会省略重复位置和状态数组；生成到 journal 的全生命周期峰值仍待测量）

### 写入与历史分离子任务

- [x] 画出现有普通放置、工作区、选区和恢复任务的阶段图。
- [x] 列出每个阶段的资源所有者：目标源、expected/before、预测 after、实际 after、journal、历史批次、内存预约和世界写入许可。
- [ ] 抽出写入期对象，只负责冲突检查、批次写入和实际结果收集。（已完成历史发布对象的第一步拆分；写入状态仍需兼容工作区与选区任务后再拆）
- [x] 抽出恢复日志会话，保证首批写入前的 write-ahead 约束，并支持失败或取消后的继续恢复。
- [x] 抽出历史发布对象，在写入完成后把实际 before/after 构建为一个 undo/redo 批次。
- [x] 先保持现有 journal 文件格式、冲突策略、坐标顺序和撤回语义不变。（现有 journal、事务顺序、重复写入和恢复策略测试通过）
- [ ] 让小型操作在必要的首批 before 与 journal 就绪后开始写入，不等待完整历史压缩。（历史压缩已后置；分段 WAL 设计草案已完成，仍需实现并验证）
- [x] 首块延迟专项子任务：设计分段 WAL 和 final seal，并定义半成品 segment 的崩溃恢复、取消和重启清理规则。（设计草案已记录于 `world_write_history.md`；manifest、segment、seal 已完成独立编码、原子写入和校验测试）
- [ ] 首块延迟专项子任务：将 manifest、segment、seal 接入实际写入路径，并补充迁移、崩溃恢复和重启清理测试。（已封存目录接入启动回放；未封存完整 segment 集合也按逆序回滚，GameTest 覆盖两者和外部修改保留。实际分段写入、进程重启清理和持久保存验收仍未完成）
- [x] 首块延迟专项子任务：用 `RecoveryJournalSegments` 校验 segment 集合的连续序号和数量，并接入启动恢复扫描。（启动校验 manifest、seal、完整 segment 集合与世界数据头；已封存目录必须完成内容回放及持久保存，才能清理。未封存目录仍阻止新写入）
- [x] 首块延迟专项子任务：在分段 WAL 落地前，禁止通过跳过冲突检查、journal 或 lease 来缩短首块等待；先保留现有安全边界。（启动扫描会拒绝缺失 manifest、seal、segment 或摘要不一致的分段目录）
- [x] 为历史压缩失败定义真实提示：世界写入已完成，但撤回记录仍在准备或无法发布。
- [ ] 为取消、写入失败、journal 失败、历史失败、世界卸载和进程重启补充资源终态测试。（普通取消、生成失败预约释放、journal 取消竞态防护、恢复转移、journal 准备失败、未封存 journal 不发布历史、分段目录完整性门禁和无 journal 历史发布取消已有覆盖；世界卸载、进程重启后的真实资源终态仍待补齐）
- [ ] 用首个方块落下延迟和总耗时对比重构前后相同场景。

## 自动化覆盖

此前验证：`gradlew --offline --no-daemon check` 与 `runGameTestServer` 均通过，GameTest 6/6 required tests passed。输出指标来自恢复操作，不证明普通放置的点击到首块延迟，也不替代客户端交互和生命周期验收。后续修改按各自的定向测试记录，不能沿用此前全量通过的结论。

2026-09-09 本轮验证：`check` 通过，`runGameTestServer` 7/7 通过。新增 `JournalRecoveryGameTests` 使用真实世界与日志文件，覆盖两条 after 修正的回滚和回放，修复修正记录尚未遍历完就误报不对齐的问题。该测试直接调用单日志恢复，不证明进程重启、启动扫描和持久保存清理全部完成。`WorldJournalPreparationCancellationTest` 覆盖取消前未调度、取消后的迟到完成与 reset、已交出日志保留。

追加验证：`runGameTestServer` 8/8 通过。新增分段目录 GameTest 覆盖 sealed after 回放、未封存 before 逆序回滚、重复执行和外部修改保留。该测试直接调用恢复入口，尚未证明真实进程重启。

- [x] 覆盖生成成功、超限、约束失败和生成异常结果。（`BlockGenerationResultTest`、各生成器测试和 `PlacementTaskTest` 已覆盖）
- [ ] 补齐普通放置、工作区和选区任务从成功、取消、写入异常到恢复创建的端到端组合测试。
- [ ] 覆盖重复位置、方块实体、流体、冲突、日志读写、世界卸载和重新加载。（重复位置、方块实体、流体恢复、恢复冲突以及日志读写/故障路径已有自动化覆盖；世界卸载/重载仍待补齐）
- [x] 覆盖重复或乱序网络请求、分片超时、断线清理和客户端重试。（请求 ID 去重、乱序分片、重复分片、超时清理和独立传输通道均有自动化测试）
- [x] 每次失败日志包含操作 ID、阶段、目标数量、批次和恢复任务状态。（放置、操作、恢复失败路径均记录 metricsSummary；其中包含 operation/phase/targets/batches 和恢复结果）

## 维护改进

- [x] 将输入事件解析与动作执行分开，统一使用一次事件快照。（`OperationInputSemantics` 与指针手势状态对象承担解析，动作执行留在输入控制器；已有决策测试）
- [ ] 将预览状态、异步生成、命中判断和绘制建立清晰的所有权边界。（预览状态已拒绝迟到 revision；异步生成取消后会校验 `BuildingPreviewKey`，旧结果不会写回缓存；仍需客户端端到端验收）
- [x] 用显式生成结果替代特殊空集合失败协议，同时保留惰性和 packed 存储。（主要生成入口已改用 `BlockGenerationResult`；旧 API 仅保留兼容适配）
- [x] 为任务阶段和资源所有权补充文档。不要用新增通用框架替代简单的交接规则。
- [x] 参考 WorldEdit 的 `EditSession`、Extent 链和 ChangeSet 思路，保留本模组的冲突检查与崩溃恢复，不复制其通用编辑器层。
- [ ] 把 `WorldChangeTransaction` 收敛为写入期状态，另设小而明确的历史发布对象。不要让普通写入调用者负责决定何时压缩、提交或释放历史。
- [x] 让 `check` 明确包含测试和 `verifyModJar`。部署只能使用已验证的 jar。
- [x] 中键或alt嵌入点击时，方块依据准星首次命中的面（入点所在平面）来计算朝向，而不是计算射线穿过嵌入方块时的出点所在平面。（`LongRangeBlockRaycast.firstCellEntry` 已统一修正入射面，并有回归测试。）
- [ ] 对于在原版中右键点击可以直接覆盖上去的方块（如积雪，矮草，似乎由一个内部方块tag控制），在快速起形或放置方块的raycast时将其视为空气，和原版放置方块的行为一致。（放置与 QuickReplace 已改用 `COLLIDER` 射线策略，选区/几何命中仍保留 `OUTLINE`；还需在游戏中用积雪、矮草等实际方块验收替换位置和朝向。）
- [ ] 选区渲染、缩放、推拉面、命中测试和预览包统一以 `min`/`max` 权威范围为输入，不得把点1/点2当作范围角点。点1/点2只由左右键更新，并在更新后用于重算 `min`/`max`；中键扩展和其他范围变换不得改写点1/点2。主路径已统一，补上了只设置单点时的空值崩溃防护，并修复点拖拽后的 min/max 缓存失效；仍需补齐复杂连续操作、退化范围、负方向和坐标缩放测试。
- [ ] 排查复杂选区操作后的客户端强制退出：任何 FastFormer 状态消息都不得触发 `clientbound/minecraft:system_chat` 编码异常；校验消息参数、组件树、长度和异常路径，并补充网络编码回归测试。
- [ ] 近距离原版交互、单击、拖拽松开、确认后的迟到事件和 Alt 切换不会误触发 FastFormer 动作。输入状态机现在是所有键盘、原版交互、鼠标和滚轮事件的第一道路由；放置、恢复、取消阶段只允许取消事件，阶段切换会统一使旧手势令牌失效。Alt 在无 FastFormer 会话时已不再写入控制器状态，服务端有限距离 raycast 也已正确应用传入范围。仍需补充真实交互验收，并继续收敛动作分支内部的业务条件，避免把业务状态重新复制成第二套输入状态机。
- [x] 删除 Smart 木头框架的屏幕调试信息；保留内部预览计算，不再向玩家 HUD 输出诊断文本。
- [ ] Smart 木框架在线框模式的线和面阶段也能根据朝向生效。
- [ ] Smart 木框架的角点跟随它连接的最长边。

## 入口

- 交互：`client/input/`、`client/session/`、`client/render/`
- 放置：`fastplace/task/`、`fastplace/FastPlaceManager`
- 世界：`fastplace/world/`、`PersistentRecoveryJournal`
- 传输：`network/transfer/`、`FastPlaceNetwork`

运行验收项目使用 [runtime_acceptance.md](runtime_acceptance.md) 的步骤和证据要求；单元测试或 GameTest 不能替代其中的客户端与服务器生命周期验证。

已有记录称 GameTest 冒烟测试、惰性直线与事务顺序检查通过，开发客户端可以进入世界。这些记录不证明上述验收完成，也不替代当前版本的验证。
