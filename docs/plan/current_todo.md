# 当前临时 TODO

这里只保存可以直接编码或验收的近期工作。用户确认后再把条目移入日期或版本计划。

## 待实现

- [x] 将 `raycast` 统一命名为“视线落点”。
- [x] 将快速起形会话中的 XYZ 直线改名为“横平竖直”，英文显示名为 `Level & Plumb`。


## 待游戏内验收

- [ ] 检查倾斜圆柱和锥柱台体的连续体积、`SOLID` 孔洞和 `HOLLOW` 外表面。
- [ ] 检查空手选区、AABB、棱柱、Alt 新建、Gizmo、变换、撤回和提交。
- [ ] 检查断线、停服、崩溃重启、方块实体、异步冲突和恢复日志。

验收发现问题后，记录复现步骤、输入状态、世界状态和日志，再把最小修复任务写入本文件。
