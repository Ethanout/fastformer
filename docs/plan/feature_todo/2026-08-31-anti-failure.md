# 失败滚蛋（2026-08-31）

本文是反失败特性的日期归档 TODO，不是完成更新日志。它记录问题、目标、阶段状态和验收项；具体实施步骤统一放在 [`current_todo.md`](../current_todo.md)。代码质量与职责拆分方案见 `.temp/2026-09-06-代码质量评估/`。

## 背景

大规模生成、放置、撤销和恢复会同时占用 JVM 内存与服务端 tick 时间。当前需要降低放置和撤销的等待感，减少内存不足导致的误拒绝，并保证失败后可以明确恢复。

## 目标

- 放置过程尽可能快，并根据服务器负载调整处理量。
- 撤销由一次输入触发，自动完成恢复，不要求再次按键。
- 不因完整事务无法一次装入内存就直接拒绝；只有最小安全批次或恢复日志无法建立时才拒绝。
- 失败、等待、回退和提交状态必须可区分，不显示与实际状态不符的“正在回退”。
- 保留语义参数协议，服务端权威重算；不可重算数据继续使用有界分片传输。

## 当前基础

- 已有生成前内存预估、快照阶段方块实体占用估算和 `operation_memory_unsafe` 保护。
- 已有持久化恢复日志、可恢复任务和世界写入许可。
- `WorldChangeBatch` 已对位置、方块状态、流体状态和方块实体数据做压缩与去重。
- 同步生成边界为 `262144`；该值只决定生成方式，不代表写入必须在一个 tick 内完成。
- 已有 `PlacementTaskPlan`、`PlacementGenerationPlan` 和 `PlacementEffect`，普通摆放与特殊摆放已经共享部分任务策略和效果入口。
- 已有 577 个自动化测试；本地 `test verifyModJar` 基线通过。测试任务可能因 Gradle 缓存显示 `UP-TO-DATE`，发布前仍需确认实际重新执行。

## 阶段状态

- [x] 增加统一操作指标和失败阶段记录。
- [x] 初步分开生成预算、写入预算和恢复预算，并提供软压力与硬拒绝分类。
- [x] 初步减少目标集合、快照、历史批次和 journal 的重复驻留。
- [x] 接入按 tick 时间、内存压力和写入耗时调整批次的基础逻辑。
- [x] 建立一次触发的撤销任务，并让大型撤销跨 tick 自动推进。
- [ ] 完成世界未加载、断线、冲突、取消和重启恢复的完整矩阵验收。
- [ ] 完成 `PlacementTask` 的资源所有权和阶段边界重构。
- [ ] 完成输入、预览和历史管理器的职责拆分；这些属于可维护性工作，不应改变世界写入语义。
- [ ] 完成对于各种失败信息的人性化和人情味翻译，语气类似自言自语（几何手的大白话风格，有面对面说话感），比如“JVM内存不足”显示为“你的电脑运存不够，因此操作被撤销了。建议多分配一些运存给mc。”

## 简短验收

- [ ] `32768`、`131072`、`262144` 和超过 `262144` 方块的放置与撤销。
- [ ] 低空闲堆、服务端卡顿、方块实体和多人并发。
- [ ] 放置失败、回退失败、世界未加载、断线和重启恢复。
- [ ] 不出现 OOM、部分提交、无日志写入或需要再次按 `Enter` 才继续的撤销。
- [ ] 每次失败都能定位 operation ID、失败阶段、目标数量及恢复任务是否创建。
- [ ] 生成失败、超限、取消和世界未加载不再依赖普通空 `Set` 的身份判断。
