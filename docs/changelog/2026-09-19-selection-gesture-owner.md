# 2026-09-19 选区手势所有权迁移

`ClientSelectionSession` 现在持有 `SelectionGestureState`。
该状态只保存一个 `SelectionDrag`，取值为面拖动或 Gizmo 拖动。
输入类原有的两个静态拖动字段已删除。旧查询入口读取当前选区会话，不保存副本。

活动拖动存在时，不再开始第二个选区拖动。
推进操作检查原记录身份、捕获身份、拖动类型和编辑令牌。
迟到更新与清理不能覆盖后来发布的状态。
会话的 `clearLiveInteraction()` 同时清空拖动、hover、交互场景和选区内容。

新增三项测试，覆盖会话隔离、清理、互斥、迟到更新、迟到清理和捕获身份替换。
此前的释放入口测试仍覆盖空拖动释放不得结束其他手势。
全量 `./gradlew test check` 实际执行通过，耗时 37 秒。

物理输入事件仍未全部入队。全局指针令牌、其他会话拖动及完整会话生命周期仍待迁移。
本次结果不替代真实客户端的退出、断线和鼠标操作验收。
