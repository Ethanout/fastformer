# 2026-09-19 公共 Gizmo 场景发布

选区交互场景现在发布 `selection_gizmo` 对象。
它是会话级控制对象，同一会话内保留身份，清理会话后更换 owner。
组件保存派生的成员实例身份、世界范围和缩放能力，不取代工作区中的权威选择集。

渲染和命中读取同一已发布对象，不再各自读取实时选择集合并公共范围。
单选、切换选择与全选入口立即发布场景。场景无变化时复用对象，只有选择变化时仍复用原部件对象。
公共按下事件从已发布成员采样，并校验当前选择集，拒绝过期目标。

公共范围、棱柱能力及按下事件测试已迁移到新路径。
新增一项控制器测试，覆盖选择后立即发布、稳定场景复用、旧快照保持与会话清理。
全量 `./gradlew test check` 实际执行通过，耗时 34 秒。

公共对象存在期间的成员变化不更换控制对象 ID，捕获通过成员实例与源数据版本防止错误操作。
统一 hover、动作绑定、完整物理事件队列和实机验收仍待完成。
