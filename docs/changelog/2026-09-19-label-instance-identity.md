# 标签实例身份迁移

## 实现范围

- 工作区维护独立的客户端实例身份，数字部件编号仍可复用。
- 移动、编辑、取消编辑和撤销保留对应部件的身份。
- 删除后新建、清空后新建及恢复持久草稿分配新身份。
- 标签命中与渲染读取同一实例身份。显示文本继续使用数字部件编号。
- 撤销快照保存实例身份，并将其计入历史保留预算。
- 网络载荷和磁盘格式不变，实例身份不持久化。

## 验证与剩余工作

- 新增四项工作区身份测试和一项标签集成测试。
- 全量 `./gradlew test check` 通过，测试实际执行。
- 尚未接入完整场景快照、hover 进入和离开事件、拖动捕获。
- 尚未执行客户端实机验收。本记录不代表交互组件阶段完成。
