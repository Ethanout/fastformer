# 选区点击动作组件迁移

选区标签、边框和 Gizmo 通过 `PRESS_BINDING` 声明类型化点击动作。按下事件保存对象快照，输入入口执行声明的动作，不保存业务回调。

hover 和点击共用场景目标解析。执行前检查对象快照、会话、部件实例和工作区状态。对象移除、替换或缺少动作绑定时拒绝事件。左右键、中键、Ctrl 和短按方向保持原语义。

新增三项测试，覆盖动作与对象身份、缺失绑定、对象替换及真实输入入口的选择分派。面编辑权限仍由原入口检查。

最终执行 `./gradlew test check` 成功，耗时 37 秒。编译与测试实际执行。`git diff --check` 通过，仅输出换行符转换提示。未执行客户端实机验收。

物理输入仍同步处理。统一事件队列、跨会话动作、hover 进入与离开动作，以及客户端实机验收仍未完成。本记录不代表整个组件迁移完成。
