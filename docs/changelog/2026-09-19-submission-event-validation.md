# 提交完成事件校验

成功回执现在在构造事件时检查观察状态。空状态以及提交中、取消中状态不能进入 mailbox。此前这些参数在收取事件时才报错，可能触发 mailbox 的失败清理并丢弃同批后续有效回执。

失败和过期回执仍允许不携带观察状态，路由恢复到提交来源。请求身份匹配规则不变。

新增两项测试：非法成功回执不能入队并妨碍后续合法回执，失败回执无需观察状态。原有非法回执测试调整异常断言范围，继续检查状态版本和请求归属未丢失。

`./gradlew test check` 实际执行通过。物理输入队列、完整会话生命周期及客户端实机验收仍未完成。
