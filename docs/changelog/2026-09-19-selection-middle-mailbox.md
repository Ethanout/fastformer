# 已有选区中键入队

2026-09-19 用户确认：已有选区的普通中键，只要输入归属选区就拦截原版选取方块。无变化、无落点和编辑失败都不让行。

`SelectionPointPress` 现在接受中键。按下与释放进入已有物理队列，编辑执行前继续检查会话、部件实例、活动部件和世界源数据。没有落点时不编辑，配对释放仍清理捕获。Alt 新建和变换后的 CreateSelection 路径保留优先级。

删除鼠标回调中“调整成功才拦截”的中键分支。对象点击与点编辑共用一次 `selectionPressRoute` 判断，不再分别计算相同的左右键路由。

规则同步至选区会话伪代码和交互规范。这个决定只适用于已有选区的输入归属，不决定空草稿中键如何建立第一点。

## 验证范围

2026-09-19 19:14 完整 `./gradlew test check` 通过，耗时 40 秒。`SelectionPointMailboxTest` 共 12 项，无失败、无跳过。本轮未重跑 GameTest。

点编辑队列测试扩展至左右中三个按钮，检查无世界校验失败、无落点及释放清理。新增中键无命中时的路由测试。队列中仍有按下、释放事件时，由 mailbox 阻止草稿恢复，不要求已经释放的物理捕获保持活动。

真实世界范围扩展、无变化点击、原版物品选取及完整鼠标操作仍需客户端实机验收。服务端初始设点和旧手势兼容路径仍待迁移。
