# 历史持久化失败状态

历史 owner 现在区分普通批次或索引发布与完整快照修复。任何异步失败都会保留 `persistenceDirty`。普通成功不会清除失败状态。只有完整快照成功且没有其他持久化任务等待时，owner 才会恢复为可淘汰状态并发送恢复反馈。

新增批次在已有持久化任务或 dirty 状态时跳过缓存裁剪，保留失败批次和后续修改，直到完整快照修复完成。

验证：历史 owner 与恢复策略测试通过。真实磁盘故障和进程中断仍需按 `runtime_acceptance.md` 验收。
