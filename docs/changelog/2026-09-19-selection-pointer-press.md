# 2026-09-19 选区按下事件采样

现有左右键命中分派已共用 `handleWorkspacePointerClick`，本次没有新增平行分派器。
新增不可变的 `SelectionPointerPress`，固定命中目标、按钮、Ctrl 状态和面短按步数。
共享分派与旧 Gizmo 入口都先采样，再处理事件。
部件选择和 Gizmo 启动不再在执行中重新读取 Ctrl。

仅部件、面和 Gizmo 命中属于该入口。空目标、创建选区目标、无效部件编号和不支持的按钮不生成事件。
新增两项测试，覆盖左右中键身份、方向、Ctrl 状态及不属于该入口的目标。
首次编译暴露一个仍使用旧签名的 Gizmo 调用点，迁移后重新验证。
全量 `./gradlew test check` 实际执行通过，耗时 34 秒。

事件目前同步处理。环境身份、目标实例校验、完整修饰键与释放事件还需在统一物理事件边界迁移。
本次不改变 Q、Enter 的时序，不将局部事件记录视为统一队列已经完成。

## 目标身份校验

按下事件记录选区会话 owner、部件实例身份和命中时的源数据对象。
公共 Gizmo 还固定当时的选择集。
采样拒绝尚未发布到交互场景的目标变化，执行前再次校验。
会话变化、部件替换、几何变化、公共选择集变化或工作区锁定后，事件不再执行。

新增三项测试，覆盖身份、几何、公共选择集与锁定变化。
其中一项直接调用输入处理入口，检查编号复用后的旧事件不会改变选择、编辑状态或已发布场景。
全量 `./gradlew test check` 实际执行通过，耗时 38 秒。
完整连接环境校验和物理事件队列仍待迁移。
