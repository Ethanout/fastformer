# TODO 完成记录（2026-09-14）

本文件收纳 `docs/plan/current_todo.md` 中已经有代码、测试或当时实机证据的历史记录。仍需要当前版本实机复验的项目继续保留在 TODO，不因这些记录而视为最终完成。

- 2026-09-14：完成三个超长文件的职责审计。报告记录混合职责、已消除和剩余重复、无效分支证据边界及后续拆分顺序。详见 [超长文件职责审计](2026-09-14-long-file-responsibility-audit.md)。TODO 删除审计项，保留实际职责拆分和等价验收。

- `SelectionOperationTask` 的 `KEEP_EXISTING` 冲突恢复、外部修改失败和 journal 预测已完成自动化修复，记录见 `2026-09-13-selection-move-conflict-audit.md`。
- 2026-09-14：移动语义遇到 `KEEP_EXISTING` 不可替换目标时会明确失败并进入既有 journal recovery；复制等不清空源的操作仍保留跳过目标语义。完整联合验收仍保留在 TODO。
- 2026-09-14：多部件组 Gizmo/外框改用合并后的 `selectionBounds()` 作为首选范围，详细方块解析受显示上限抑制时仍能保持组级显示。客户端位置与堆叠体验验收仍保留在 TODO。
- 2026-09-15：工作区外框、Gizmo 中心和命中范围统一使用变换后的完整选择范围。含空气边界的选择不再退回实际方块范围。定向测试覆盖非空方块快照与更大的选择范围。客户端画面和命中体验仍由 BUG-01、BUG-07 和 BUG-11 跟踪。
- 2026-09-14：分帧 building 预览改用会话内累积集合和只读视图，批次合并不再重复复制全部历史方块；revision 变化时清空集合。连续移动帧时间和取消释放仍需实机验收。
- 2026-09-14：building 渲染派生数据按 `BuildingRenderKey` 合并缓存。稳定帧直接复用控制点、特殊样式、marker、状态覆盖表和最终 shell membership，不再重复创建集合或执行全量集合比较。生产外壳缓存直接接收不可变 render frame，省去第二次防御性复制；公共缓存构造仍保留复制保护。定向缓存测试通过，客户端帧时间仍由 TODO 跟踪。
- 2026-09-14：`FREE_SCROLL` 坐标反馈改为会话级常显。首点确认后锁存最近偏移，滚轮请求引起的短暂空点快照继续显示同一数值；完成线阶段、切换模式、取消、失焦或换世界时清理。`PreviewFeedbackStateTest` 覆盖 offset 保存和清理边界；客户端无闪烁验收仍保留在 TODO。
- 2026-09-14：修复 `FREE_SCROLL` HUD 在滚轮快照过渡期间的闪烁路径。活动会话在空点快照时继续刷新并锁存最近偏移；短暂 inactive 快照不再立即切断反馈，待后续明确阶段/模式变化或生命周期清理。`./gradlew.bat test`、`check` 与 `deployTo233` 通过；BUG-17 仍需客户端连续滚轮验收。
- 2026-09-14：`FREE_SCROLL` HUD 进一步锁存最近一次完整文字数据。同一会话每帧刷新、相同坐标重复刷新或偏移变化时不再清空锁存值；服务端快照短暂缺少可绘制数据时继续显示最近坐标。`PreviewFeedbackStateTest` 覆盖这些路径，最终客户端验收仍由 BUG-16/17 跟踪。
- 2026-09-14：`FREE_SCROLL` HUD 总门禁改为先刷新会话锁存，再判断是否跳过渲染。inactive 过渡不再按滚轮淡出计时器结束锁存；活动的其他模式、取消、失焦和换世界仍清理。`PreviewFeedbackStateTest` 与完整 `check` 通过，客户端验收仍由 BUG-16/17 跟踪。
- 2026-09-14：修复任务状态对 `FREE_SCROLL` HUD 的绘制门禁。滚轮请求触发短暂任务快照时，会话 HUD 仍保持隐藏，但锁存的坐标文字继续以满透明度绘制。`PreviewFeedbackStateTest` 通过并完成 Java 编译；连续滚轮的客户端验收仍由 BUG-16/17 跟踪。
- 2026-09-14：修正移动成功回执的源遮罩收尾顺序。成功路径在丢弃旧遮罩前只恢复源/目标交集处的已提交方块及方块实体，其余源位置保持空气；不再先扫描工作区并把服务端新状态误当成旧源方块隐藏。交集筛选和控制器定向测试通过，客户端时序验收仍留在 BUG-02。
- 2026-09-14：`SourceBlockRenderMask.reapply()` 在重新隐藏服务器/外部非空气更新前保存最新状态和方块实体 NBT，失败或取消时恢复最新观察值，而不是最初选择快照。完整 `check` 与 `.233` 部署通过；空气更新和粒子竞争仍由 BUG-02 实机验收。
- 2026-09-14：源方块遮罩改用不可见的 `VOID_AIR` 作为客户端占位符。服务器在遮罩期间发来的普通空气不再与占位符混淆；取消或失败时保留服务器删除结果，不会恢复旧源方块。非空气更新仍保存为最新恢复状态。`SourceBlockRenderMaskTest` 通过；方块实体包顺序和粒子竞争仍由 BUG-02 实机验收。
- 2026-09-14：失败目标坐标也参与工作区面和边框命中。射线命中失败方块对应的表面区域时，客户端不再返回普通面或部件命中；其他可见区域、含空气边界和整体 Gizmo 保持可用。`WorkspaceInteractionResolverTest` 通过；局部失败回执与重试仍由 BUG-12 实机验收。
- 2026-09-14：统一取消键入口显式清理 `PreviewFeedbackState`，避免取消后残留 free scroll 坐标或 Gizmo HUD 状态被下一会话继承。客户端取消流程仍需最终实机验收。
- 2026-09-14：客户端输入 tick 在玩家或连接失效的早退路径也显式清理瞬时反馈，避免断线/换世界事件顺序造成 HUD 状态残留。断线重连完整流程仍需实机验收。
- 2026-09-14：非 cuboid 选择体在旋转、缩放或重复变换后，即使详细方块解析被显示上限抑制，也保留基于选择范围的保守交互 AABB，避免外框和 Gizmo 因空解析结果消失。新增 `WorkspaceSelectionBoundsTest` 回归覆盖；客户端位置验收仍保留在 TODO。
- 2026-09-14：接通非 cuboid 旋转范围到工作区渲染入口。`WorkspaceInteractionResolver.selectionBounds()` 不再只让 cuboid 使用 `WorkspaceSelectionBounds`；非 cuboid 详细方块受显示上限抑制时，外框和 Gizmo 仍取得旋转后的世界范围。`WorkspaceSelectionOutlineTest` 和 `WorkspaceSelectionBoundsTest` 通过；客户端旋转画面仍保留在 BUG-01。
- 2026-09-14：选区范围静态约束完成归档：渲染、缩放、堆叠和放置计算使用 `min/max`，`point1/point2` 仅作为输入锚点；`CuboidSelectionSession` 的边界运算采用饱和算术并有回归测试。服务端/客户端联合验收继续保留在 TODO。
- 2026-09-14：工作区堆叠 stride 改为按变换后的选择框 `min/max` 范围计算。单部件不再用非空气方块宽度回退，多部件按合并选择框作为一个整体；工具函数和控制器手势测试均覆盖含空气边界场景。客户端滚轮方向与各轴间距仍保留在 TODO。
- 2026-09-14：锁定部件的普通部件命中、面命中、持续拖拽和面高亮均统一阻断；已选锁定部件仍保留部件/组 Gizmo 整体变换能力，外框使用完整选择范围。客户端显示和交互验收仍保留在 TODO。
- 2026-09-14：`HoverDwellTracker` 不再线性爬升倍率；同一目标停留达到配置阈值后立即使用上限倍率，阈值前保持 `1x`，切换目标、清理和时钟回退仍恢复 `1x`。单元测试覆盖正负时间起点及阈值边界；BUG-15 仍需客户端确认实际计时与距离递减体验。
- 2026-09-14：普通 building 预览不再绕过距离透明度计时器。操作预览、工作区和选区会话仍保持完整可见。静态方块面缓存改为上传中性 alpha，只在绘制时应用当前透明度，避免首次上传值被重复计算或长期缓存。`InteractionContextTest`、`DisappearanceStateTest` 和 `BuildingShellFaceBufferTest` 通过；最终客户端计时体感和画面仍由 BUG-15 跟踪。
- 2026-09-14：工作区提交的 30 秒超时和回执匹配判定提取为无副作用策略。第 600 tick 解锁并保留可重试草稿；旧 transfer ID、已结束请求和缺失 ID 的回执不能改变当前状态。`ClientOperationControllerTest` 覆盖超时边界和迟到回执判定；正常慢任务、真实无回执和重新提交仍由 BUG-10 实机验收。
- 2026-09-14：计时器调整后再次运行 `./gradlew.bat runGameTestServer --no-daemon`，44 个必需 GameTest 全部通过；该结果只证明服务端回归稳定，不替代客户端计时体感和预览画面验收。
- 2026-09-14：`DisappearanceState` 对同一目标的靠近和远离都使用停留加速，目标切换首个 tick 恢复 `1x`，透明度随计数变化。新增靠近/远离和目标切换测试，`./gradlew.bat check deployTo233 --no-daemon` 通过；BUG-15 客户端体感仍保留在 TODO。
- 2026-09-14：普通 building 预览加入会话级可见性门禁，活动预览不再被近距离原版淡出逻辑压到零透明度；该修复针对“只剩 outline 直线”的路径，仍需客户端画面确认。
- 2026-09-14：修复普通 building 首点/候选点被从 shell 集合过滤的问题。单方块预览现在保留方块面，同时继续绘制控制点和外框；BUG-13 仍需客户端确认首点及多点场景。
- 2026-09-14：building confirmed/pending 外壳面增加独立静态 GPU 缓冲。稳定 mesh 不再每帧重建和上传面顶点；相机位置变化时只重排并上传透明四边形索引，呼吸透明度由 shader 颜色动态传入。mesh 变化、lightweight 切换、取消和世界结束均沿用统一缓存清理路径释放显存。定向缓存测试通过；远坐标、透明排序和重复取消后的显存释放仍保留在 TODO 实机验收。
- 2026-09-14：修正静态外壳缓冲的跨层透明排序。confirmed 和 pending 同时存在时，渲染器把两层面放回同一透明批次并做全局距离排序；单层场景继续使用静态缓冲。该路径保留两层不同透明度和 pending 呼吸效果，混合层性能与画面仍由 TODO 实机验收。
- 2026-09-14：大结构的 lightweight 决策发生在完整方块物化之前。LINE、PATH、PLANE、VOLUME 先从点集做饱和 workload 估算；超过 100,000 扫描单元时不启动完整生成，仅使用最多 4,096 方块的 outline fallback。新增策略边界测试，最终跟手性和首帧时间仍由 BUG-04/13 实机验收。
- 2026-09-14：快速起形提交资格移入 `FastPlaceSession`。`FREE_SCROLL` 调整候选后，服务端拒绝未确认候选的 Enter 提交并保留草稿；确认线或切换阶段后恢复可提交。快速滚轮回到零偏移仍需确认，其他线模式不继承该状态。`FastPlaceSessionSubmissionTest` 与 `ClientInputStateMachineTest` 通过；完整客户端时序仍保留在 TODO。
- 2026-09-14：提交资格和透明排序修正后运行 `./gradlew.bat runGameTestServer --no-daemon --console=plain`，44 个必需 GameTest 全部通过。失败日志来自测试注入，最终批次成功；客户端输入和透明画面仍需最终实机验收。
- 2026-09-14：近距离原版状态门禁新增操作选区预览状态判断，AABB 只确认一个点且工作区为空时仍由选区会话持有输入；实机首点阶段、取消和确认后的切换继续保留在 TODO。
- 2026-09-14：空手且命中可达方块时，`InteractionContext.nearVanillaBlock()` 在没有操作选区点收集会话时让行原版破坏；AABB 首点等待第二点仍保持选区输入门禁。无会话、普通起形和选区场景仍需 `.233` 实机验收。
- 2026-09-14：placement 与 workspace 请求令牌从 `ClientInputStateMachine` 的手势字段中拆出，由 `ClientRequestTracker` 单独持有。状态机只协调提交阶段转换；重复请求、错误令牌、旧回执和世界会话清理不能改变当前请求所有权。`ClientRequestTrackerTest` 与 `ClientInputStateMachineTest` 通过，输入等价性仍由 TODO 跟踪。
- 2026-09-14：操作工作区支持断线和客户端重启后的草稿恢复。版本化 NBT 保存选择点、`min/max`、prism/hull 几何、部件、变换、堆叠、源快照、锁定基线、方块状态和方块实体，不保存 Alt、焦点、拖拽、锁定状态、撤销历史或提交等待。文件通过临时文件和原子替换写入，并按远程服务器地址或单人存档路径、玩家和维度的 SHA-256 作用域隔离。首次连接或同进程重连时，只有服务端选择身份匹配且玩家确认后才恢复；错误选择、拒绝恢复、无活动服务端会话和等待超时会丢弃草稿。`ClientOperationDraftCodecTest`、`ClientPlayerSessionTest`、工作区和控制器测试通过；真实重连仍保留在 TODO。
- 2026-09-14：活动工作区现在始终持有左键操作。源方块因服务器更新短暂可见且准星未命中 Gizmo/面时，不再让行原版破坏并产生破坏粒子；无工作区时的空手近距离原版让行保持不变。`OperationInputSemanticsTest` 覆盖两种所有权边界，最终竞争时序仍由 BUG-02 实机验收。
- 2026-09-14：完成[失败提示矩阵](2026-09-14-failure-feedback-matrix.md)。矩阵覆盖普通放置、工作区、选区、撤回/重做、恢复、断线、Q 取消、重启和存储故障，并记录原因、当前结果、玩家下一步和代码入口。泛化矩阵任务从 TODO 移除，具体实机触发仍保留在各验收项。
- 2026-09-14：客户端草稿改用严格 NBT 读取。损坏文件不再静默视为空文件；客户端会删除损坏文件并显示结果。保存或删除失败也显示原因和下一步。换维度时删除已加载的旧维度草稿，避免之后误恢复。编解码器拒绝未知枚举、非有限变换，以及缺少方块注册表的方块数据。剪贴板继续保留原有容错语义。`OperationClipboardStoreTest`、`ClientOperationDraftCodecTest` 和会话定向测试通过；真实重启、换维度和文件权限故障仍由会话恢复实机验收覆盖。
- 2026-09-14：`ClientInputStateMachine` 使用集中式 `State × InputKind` 分派表。完整矩阵测试覆盖每个状态和输入类型，并证明被拒绝的事件不改变状态、generation、手势或提交 owner。客户端输入等价验收仍保留在 TODO。
- 2026-09-14：提交完成入口使用显式 `SubmissionEvent` 区分成功、失败和过期。placement 与 workspace 只接受当前 owner 的 token；旧结果不能改变状态或释放新请求。定向状态机和控制器测试通过。
- 2026-09-14：`ClientInputStateMachine` 保存提交起始阶段。成功事件采用匹配回执后的服务端观察状态；失败和过期事件忽略过渡快照并回到原提交阶段，保留可重试草稿。定向状态机测试通过。
- 2026-09-14：普通起形初始阶段的 shell 兜底改为识别“服务端已确认 0 或 1 个点”。确认首点后，即使当前候选使预览点列表变为两个点，详细 shell 延迟时仍绘制首点和候选方块框。`FastPlaceClientPreviewCoreTest` 覆盖首点、候选、详细 shell 就绪和第二点确认边界。
- 2026-09-14：普通起形的初始方块 fallback 只在详细方块面实际可见后关闭。异步 shell 仅有边线时不再被误判为完整预览，因此首点和候选方块框不会因 outline 先到而消失。相关预览测试通过，最终画面仍由 BUG-13 实机验收。
- 2026-09-14：增加 `PreviewRenderOwner`。模式切换期间若新的 building 会话已 active，而旧 operation 快照尚未失效，building 立即取得世界预览和放置 raycast 所有权。未开始的 building 设置不会盖住工作区，geometry 保持原有最高优先级。定向所有权测试通过。
- 2026-09-14：普通放置的内存预约估算改为按派生几何计算。直线使用 Bresenham 长度，线框面和棱柱使用边长上界，实体与空心形状使用派生包围盒，多边形体积纳入顶面或 apex。所有运算饱和并由生成上限封顶；拒绝日志补充阶段、填充模式、点数和各项估算。边界测试覆盖 1、3、16 方块、斜线、面、棱柱、多边形及极端坐标。
- `FastPlaceClientInput` 左右键工作区指针分派已抽取共同路径。
- `FastPlaceClientPreviewCore.endWorldSession()` 的 building 缓存清理已与统一清理路径收拢。
- `renderPendingBlocks` 空输入分支重复清理 `PENDING_GHOST_CACHE` 已移除；该项仅为静态重复逻辑清理，未改变渲染行为。
- `ClientOperationController` 的 prism 草稿推进已抽取共同完成逻辑。
- 预览替换、取消和迟到异步结果隔离已实现。
- 历史缓存淘汰的饱和字节计算、内存异常 FIFO 待记录和历史保留期限已实现。
- 2026-09-15：修正无帧率限制 GameTest 中异步选择提交的测试时限。两个依赖 journal 与 history 压缩的选择操作测试将上限从 `20000` tick 调整为 `100000` tick，避免测试在异步压缩完成前退出并遗留 placement lease。完整 GameTest 连续两次通过，47 个必需测试全部成功。随后完整 `check` 与 `.233` 部署通过。
- 2026-09-15：复核最终验收清单的代码入口。BUG-01 至 BUG-17、raycast、更新策略、木框架方向和小结构内存预约均已有实现与自动化证据，没有发现独立的代码缺口。木框架客户端预览与服务端放置共用 `SmartWoodFrame.resolve` 效果链。剩余项目需要客户端、真实服务器、性能或进程中断证据，因此继续保留在 TODO。
- 方块更新策略已引入 `PlacementUpdateMode`，`CLIENT_ONLY` 为默认安全策略，`NORMAL` 可显式启用。
- 2026-09-14：普通世界撤回和重做在任务开始时保存玩家的 `PlacementUpdateMode`，并在历史写入中使用该模式。恢复任务与失败回滚仍固定使用 `CLIENT_ONLY`，避免事务恢复触发邻居更新。策略测试与设置测试通过，最终行为仍由“方块更新策略”实机验收。
- 2026-09-14：重启后的近期历史增加异步读取路径。登录时按玩家条数上限和 256 MiB 总预算读取索引与批次，读取期间阻止新的编辑；损坏或不兼容数据会保留磁盘文件并阻止覆盖。内存中较新的 operation ID 优先，磁盘中的旧记录按索引顺序追加且去重。单元测试和 GameTest 源码覆盖合并及真实异步落盘后读取；超预算旧历史的按需加载仍在 TODO。
- 木框架沿线放置方向、控制点与 Gizmo 悬浮文字接口、Enter 语义路由和相关自动化测试已完成静态实现。
- 2026-09-14：操作选区面的准星 HUD 改为消费解析出的 `OperationInteractionIntent.Face`。可调整面由命中对象统一提供“推/拉”两行文字，其他目标沿用单行文字；缺失或空行会在测试或运行时明确失败。`InteractionIntentResolverTest` 覆盖多行命令。
- 2026-09-14：工作区预览不再用纯色 shell 替换相邻完整方块。所有非待删除部件，包括变换后锁定部件，继续渲染原始方块模型。统一虚拟预览世界为跨部件的原版侧面剔除提供邻接状态。源方块遮罩不再过滤目标虚影，因此源目标重叠区域仍显示新预览。`OperationPreviewRendererTest`、遮罩测试与渲染定向测试通过，最终画面仍由 BUG-02/03/07 实机验收。
- `point1/point2` 不再作为渲染、命中、拖拽、预览和提交计算的数据源，相关范围计算使用 `min/max`。
- 大尺度快速起形保留低成本外线框，普通起形在分帧生成期间保留可见边界。
- 历史记录、任务生命周期和 41 个 GameTest 的自动化回归已通过。
- 2026-09-14 再次运行 `./gradlew.bat runGameTestServer`，44 个必需 GameTest 全部通过；日志中的异常均来自专门注入失败的测试，最终测试批次成功完成。
- 2026-09-14：新增关服边界回归测试 `shutdownKeepsSealedJournalForStartupReconciliation`。测试覆盖 batch 已发布、journal 已封口、history index 被阻塞后调用关服等待和清理；确认 journal 与 batch 在关服阶段保留，并可由启动 reconciliation 修复索引后清理。完整 GameTest 共 45 个必需测试全部通过。真实进程中断和磁盘故障验收仍保留在 TODO。
- 2026-09-14：归档 BUG-08 的静态约束。选区变换、渲染、命中、预览和提交均以 `min/max`、部件模型和变换数据为派生源，`point1/point2` 只保留为输入锚点；范围和编解码回归测试已覆盖。重连实机行为继续由“会话恢复实机验收”跟踪，不再重复占用单独 BUG 条目。
- 2026-09-14：修正普通起形首次启动的 Raycast 环境选择。客户端不再用鼠标中键隐式请求嵌入；未按 Alt 时固定为 `SURFACE`，按住 Alt 时为 `EMBEDDED`。新增纯输入策略和两种模式的回归测试；预览与最终落点一致性仍保留在客户端验收。
- 2026-09-14：距离透明度计时器的停留目标改为 FastFormer 长距离 Raycast 命中。远距离目标不再因原版短距离 `hitResult` 为 `MISS` 而永远停留在 `1x`；同一远近目标持续 100ms 后均可进入 `3x`，切换目标仍由 `DisappearanceState` 还原基准倍率。定向计时和上下文测试通过，最终体感仍由 BUG-15 客户端验收跟踪。
- 2026-09-14：旧历史按需加载完成第一阶段基础。`LoadedHistory` 同时返回完整 durable undo/redo 顺序和当前已解码前缀，避免内存预算截断后丢失磁盘尾部身份；新增受条数与字节预算约束的异步 `loadPage`。`HistoryOrderCatalog` 固定内存前缀与磁盘尾部合并、undo/redo 头部迁移、新操作清空 redo、条数裁剪和顺序不一致拒绝规则。45 个必需 GameTest 及目录模型单元测试通过；管理器自动触发下一页前，TODO 的按需加载项保持未完成。
- 2026-09-14：旧历史按需加载管理器接线完成。请求数量超过内存前缀时，管理器按 32 条和 64 MiB 上限异步读取旧页；合并页面后重新执行完整请求检查，不能绕过维度、操作权限、世界写锁或方块冲突。GameTest 覆盖部分内存、磁盘旧页、加载期间撤销权限、重试和完整 redo 顺序。46 个必需 GameTest 全部通过，客户端与故障场景实机验收仍保留在 TODO。
- 2026-09-14：明确历史 owner 的缓存淘汰边界。活动任务、`pendingCaptures`、`pendingRecords`、未完成索引写入、失败写入重试和 durable commit 均阻止 owner 淘汰。定向压力测试覆盖这些状态，完整单元测试和 46 个必需 GameTest 全部通过。磁盘故障和真实进程中断仍由 TODO 单独跟踪。
- 2026-09-14：补齐工作区虚影与真实世界之间的原版剔面。统一预览世界在虚影坐标使用跨部件状态，在其他坐标读取客户端世界，不再把真实邻居视为空气。GameTest 覆盖完整方块共享面、台阶保留外露面和相同玻璃共享面；47 个必需 GameTest 全部通过。BUG-03 的最终画面仍需客户端验收。
- 2026-09-14：修正选区会话后普通起形首点错位。服务端现在以 `StartPlacementPayload.embedded` 作为首点的权威 modifier 状态，普通请求会清除选区阶段残留的 Alt 状态，嵌入请求也不再依赖旧状态。首点只产生一次预览同步，后续点继续读取实时 Alt 状态。完整 `check` 和 47 个必需 GameTest 通过；BUG-13 仍需客户端确认选区切换后的线框与方块面位置。
- 2026-09-14：修正 free scroll 坐标提示在滚轮快照切换时闪烁。有效 free scroll 会话持续显示最近坐标。滚轮请求后的 1 秒过渡保护会忽略短暂 active 非 free-scroll 快照。短暂空快照、滚轮停止、数值不变和快照延迟均保持常显。取消、失焦、换世界和断线仍会立即清理提示。真实模式切换在保护期结束后清理提示。`PreviewFeedbackStateTest`、`HudFadeTimerTest` 及完整 `check` 通过；BUG-16/17 仍需客户端确认实际画面。
- 2026-09-14：修正移动源方块遮罩的区块加载竞态。源区块在首次遮罩时尚未加载的情况下，后续客户端 tick 会在真实方块到达后保存最新状态并重新遮罩，不再要求该位置已有旧快照。取消时仍恢复最新服务端状态。定向遮罩测试通过；BUG-02 的区块重载和服务器更新画面仍需客户端验收。
- 2026-09-14：工作区多个部件落在同一坐标时，只由工作区顺序中的最后一个部件绘制该方块。渲染所有者与统一遮挡状态一致，避免同坐标虚影重复绘制和重叠面闪烁。相邻坐标仍通过统一预览世界使用原版剔面。`OperationPreviewRendererTest` 覆盖重叠坐标所有权；BUG-03 的最终画面仍需客户端验收。
- 2026-09-14：普通起形的端点 fallback 改为分别检查 confirmed 和 pending 详细面。首点详细面先到时，缺失的候选方块框不再提前消失。确认第二点后，详细线段生成前仍显示两端方块框。`FastPlaceClientPreviewCoreTest` 覆盖分层到达和第二点过渡；BUG-13 的最终画面仍需客户端验收。
- 2026-09-14：切换到 `FREE_SCROLL` 时，服务端先清理旧模式状态，再写入客户端当前候选偏移。候选不再被 `onModeChanged()` 立即清零，并继续要求确认后才能提交。`FastPlaceSessionSubmissionTest` 覆盖候选保留和普通模式清理；BUG-16/17 的最终交互仍需客户端验收。
- 2026-09-14：世界卸载只通过 `endWorldSession()` 清理一次源方块遮罩。building fallback 和 at-limit 状态继续由统一的预览生成取消路径清理，不存在旧审计所述的缓存遗漏。最终换世界画面仍由预览缓存实机验收跟踪。
- 2026-09-14：工作区提交失败坐标绑定到产生回执时的部件草稿。选择和锁定变化不影响失败虚影过滤；移动、缩放、旋转、堆叠、粘贴、删除、撤销或重做改变草稿后，旧失败坐标不再隐藏新草稿。成功、清空和断线仍清理失败状态。`ClientOperationControllerTest` 覆盖相同草稿保留和变更草稿失效；BUG-12 最终画面仍需客户端验收。
- 2026-09-14：归档阶段草稿和预览 revision 的非法事件不变性测试。`ClientInputStateMachineTest` 遍历完整 `State × InputKind` 矩阵，并确认拒绝事件不改变状态、generation、手势或请求 owner。`ClientPreviewStateTest` 覆盖拆分预览的旧 revision、错序到达、重复快照和完整快照水位。对应维护项从 TODO 删除。
- 2026-09-14：geometry 预览增加服务端单调 revision 和客户端水位。客户端拒绝迟到或重复的 geometry 包，且旧包不再清理当前变换基线或渲染缓存。断线和服务端清理会重置对应水位。协议编解码与客户端状态测试覆盖 revision 保留、迟到 inactive 包和重复包；其余 owner 与生命周期边界继续由 TODO 跟踪。
- 2026-09-14：operation 预览的唯一同步入口拒绝重复 revision。等待用户确认的重连快照重复到达时，不再绕过确认并直接恢复工作区；普通重复包也不再重复发布客户端状态。`ClientOperationControllerTest` 覆盖两条路径，其余 owner 与实机重连仍由 TODO 跟踪。
- 2026-09-14：活动状态包增加服务端单调 revision 和客户端水位。迟到的 `NONE` 或旧会话状态不能覆盖较新的活动状态，重复包也不再增加客户端预览版本。协议编解码和 `ClientPreviewStateTest` 已覆盖；跨维度 owner 与真实网络时序仍由 TODO 跟踪。
- 2026-09-14：统一 building 外壳面的距离透明度。confirmed 与 pending 同时绘制时不再在基础透明度和顶点写入阶段重复乘世界预览透明度；单层静态缓存和混合透明批次现在都只应用一次计时器结果。`FastPlaceClientPreviewCoreTest`、`DisappearanceStateTest` 和 `InteractionContextTest` 通过；最终变化速度和画面连续性仍由 BUG-15 实机验收。
- 2026-09-14：复制、粘贴和工作区提交的 actionbar 提示选择移入 `ClientInteractionFeedback`。`FastPlaceClientInput` 只传递操作结果，不再读取和选择失败提示键。`ClientInteractionFeedbackTest`、输入状态机测试和控制器测试通过；提示的客户端可见性与顺序仍保留在最终验收清单。
- 2026-09-14：工作区提交等待改为三阶段状态。客户端等待服务端任务启动最多 30 秒；收到 `OPERATION_TASK` 后不再对正常慢任务计时解锁；任务活动结束后等待最终回执 2 秒。超时继续保留草稿，且旧 transfer ID 不能结束新请求。`WorkspaceSubmissionTrackerTest`、控制器、活动 revision 和预览状态定向测试通过；真实慢任务、无回执和断线重连仍由 BUG-10 实机验收。
- 2026-09-14：输入阶段对象开始持有自身的分派、提交资格、服务端观察和提交结束规则。`ClientInputStateMachine` 删除集中式策略表，只保留请求 owner、状态协调和手势 generation。完整 `State × InputKind` 矩阵、请求令牌、操作语义和交互意图定向测试通过；物理输入等价性仍由最终客户端验收跟踪。
- 2026-09-14：删除已失效的 raycast 设置。普通放置固定使用 `SURFACE`，按住 Alt 时通过运行时 `RaycastPlacement.EMBEDDED` 请求嵌入。设置界面和设置协议不再显示或传输该选项；旧 NBT 键会被忽略且不再写回。设置默认值、旧 NBT、设置包和开始放置协议测试通过；最终落点仍由客户端验收跟踪。
- 2026-09-14：世界会话结束顺序移入 `PreviewSessionLifecycle`。输入、源遮罩、预览状态、工作区、命中缓存、HUD 反馈和世界渲染状态仍由原组件持有，生命周期对象只协调清理顺序。状态所有权和失焦反馈测试通过；真实换世界、断线和重连仍由 TODO 跟踪。
- 2026-09-14：历史恢复的更新模式、方块冲突、部分回滚、取消和重试保留规则移入无状态的 `HistoryRecoveryPolicy`。`WorldHistoryManager` 继续协调任务和持久化，不再内联这些恢复决策。恢复策略测试通过；真实磁盘故障和进程中断仍由 TODO 跟踪。
- 2026-09-15：预览 HUD/反馈、缓存、几何命中和世界清理已有独立组件。`PreviewFeedbackHud` 负责 free-scroll 锁存、坐标格式、淡出状态和绘制；缓存、命中与世界会话清理继续由各自组件负责。状态和格式测试通过；客户端等价性继续由预览缓存及最终画面验收跟踪。
- 2026-09-15：键盘快捷键、Enter 提交和滚轮输入已有独立语义决策。物理入口只收集上下文并执行已接受命令；阶段对象给出有效性和拒绝原因。定向输入测试通过；鼠标按键、中键、Alt、拖拽和取消仍留在 TODO。
- 2026-09-15：历史旧页的加载选择、方向和恢复请求计数移入 `HistoryPageLoadPlan`。管理器继续持有异步存储、批次合并和世界写入。计划测试通过；历史调度、磁盘存储和内存缓存的完整拆分仍留在 TODO。
- 2026-09-14 之前的实机“通过”记录仅作为历史证据。后续用户报告已重新打开的项目仍以 TODO 中的未完成条目为准。
