# 快速起形提交等待

Enter 捕获当前草稿版本和提交数据。普通模式只取已确认点，FREE_SCROLL 线模式保留候选。客户端后台生成并校验，成功后只发送一次。等待期间禁止修改形状，Q 取消尚未发送的意图，迟到结果不能发送。校验失败后需要重新按 Enter。

提交意图、计算计划和传输协调分别归入 QuickShapeSubmissionIntent、PlacementGeometryPlan 和 QuickShapeSubmissionController。服务端发布同版本材料、上限和面偏好，收到请求时拒绝已变化的条件。服务端仍重新生成几何，不代表客户端生成后由服务端校验的完整架构已经完成。

请求发出后到回执到达前，取消仍识别快速起形来源，不清空无关选区部件。伪代码的取消范围统一引用已有退出规则。

f05fbdd 的完整 test、check、runGameTestServer 通过，90 项 GameTest 通过。后续取消来源测试覆盖等待期间来源保留、取消清理及选区提交隔离。客户端实机验收、输入后预览往返延迟和完整本地候选迁移仍待完成。
