# 快速起形阶段输入规则

用户确认左键类似会话内 Ctrl+Z，逐级回退，多边面和多面体每次撤回一个点。坐标平面与基准面中键确认第三点后立即写入世界。伪代码删除对应待定项，总则保留其他会话尚未确认的规则。

FastPlaceSession.undoStep 修正闭合底面只取消闭合、不撤点的分支。现在删除最后一个底面点并重新进入面阶段。已确认高度时先删除高度点，保留闭合底面。

随后核对回退资源发现候选偏移和测量锚点仍可能引用上一阶段。撤点现清理候选偏移；锚点所指的点被删除时清空锚点，让几何计算使用现有点的默认锚点。高度点回退也经过同一清理路径。新增回归测试及 FastPlaceSessionTieBiasTest 通过，日志为 `.dsh-tmp/quickshape-undo-state.log`。这项后续修改尚未重跑 GameTest。

QuickShapeInputRules 集中声明多边面中键无操作。客户端消费中键，不让给原版。服务端在补点或提交前执行相同检查，避免直接请求绕过客户端。

新增三项逐点回退测试、两项中键阶段规则测试和一项服务端中键测试。服务端测试在创造模式、输入门禁开放且朝向空中的条件下检查草稿保留、点数不变和没有写入任务。完整 `test check runGameTestServer` 通过，88 项 GameTest 全部通过。强化中键测试前置条件后的再次 GameTest 也通过。日志为 `.dsh-tmp/quickshape-stage-rules.log` 与 `.dsh-tmp/quickshape-middle-gate.log`。

用户同时确认 Enter 计算中等待完成后自动提交一次，Q 可取消尚未发送的意图。伪代码和 TODO 已更新。这一确认没有完成客户端提交意图实现。完整鼠标队列迁移与客户端实机验收仍未完成。
