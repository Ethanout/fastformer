# 空手势与迟到释放

- 取消手势后，活动令牌清零。下一次开始仍使用递增序号。
- `NONE` 和空类型不能通过活动检查或归属检查。
- 迟到释放不能结束替代手势。重复释放与重复取消保持无活动状态。
- 非法开始不能改变当前手势。

## 验证范围

- 新增三项手势状态测试。全量 `./gradlew test check` 通过。
- 生产调用没有依赖取消后的非零令牌，也没有把 `NONE` 当作活动手势。
- 此次只收紧现有守卫，不代表统一对象捕获或物理输入队列完成。
- 仍需客户端实机验收。
