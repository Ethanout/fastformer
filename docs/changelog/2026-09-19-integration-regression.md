# 2026-09-19 集成回归记录

## 版本与命令

- 目录：`D:/重要的资料/项目/__mc/fastformer`。
- 分支基线：`bac71b8`。工作树包含未提交修改，不能仅用该提交标识运行版本。
- 命令：`./gradlew runGameTestServer`。
- 运行范围：网络队列、输入路由、标签组件、选区场景、hover 生命周期、编辑令牌及空手势调整后的工作树。

## 自动化证据

进程退出码为 0，Gradle 报告 `BUILD SUCCESSFUL in 36s`。
日志 `run/gameTestServer/logs/latest.log:3442` 在 2026-09-19 05:20:37.938 报告：

```text
All 81 required tests passed :)
```

该日志会被后续运行覆盖。这里保留本次结果，不将 HEAD 当作完整运行版本。
测试中的故障注入会产生失败操作日志，不能单独据此判断测试失败。

## 验收边界

本次结果证明现有 81 项必需 GameTest 通过，不证明所有重构任务完成。
客户端显示、hover、原版输入拦截和性能采样仍需实机验收。
真实进程中断、磁盘满和恢复验收仍需单独留证。
不改变 TODO 中各阶段的实机状态，也不将旧版本 bug 记为本轮修复。
