# 2026-09-11 已完成 TODO

## 多边形解析轮廓

多边形预览接入直接由控制点生成的边线。未闭合路径不补闭合边，闭合面显示底边，拉伸体显示上下底边和连接边，锥顶体使用与实际生成器一致的法向高度及顶点位置。高度点不再混入底面轮廓。大预览降级与线框模式均使用该入口，不枚举轮廓上的全部方块。

GPT-5.6-sol low 完成几何辅助方法与四项测试，主代理核对生成器并接入渲染。`check` 通过。实际画面、密度显示及帧时间验收仍待完成。

## 外壳缓存只发布成功结果

外壳网格构建成功后才更新缓存键。形状或样式处理抛异常时保留之前的键与网格，下一次相同请求会重新构建，不会把旧网格误认为新结果。

`check` 通过。新增测试连续两次触发构建异常，确认失败请求不会命中缓存，原请求仍可复用原网格。该测试验证缓存发布顺序，不代表客户端异常提示或完整预览验收完成。

## 大预览边线与超限取消

扫描估算超过十万格的非多边形起形预览复用解析几何边线，轮廓方块超限后仍显示几何框架。增量生成达到预览上限时取消进度桥和后台任务，不再持续消费已经不会显示的结果。

`check` 通过。本次没有新增实机证据。多边形轻量轮廓、密度显示及各形状与实际方块边界的画面对照仍待完成。

## 大扫描预览提前降级

快速起形的扫描估算超过十万格时，候选预览和已确认预览直接走轮廓降级，不先生成完整方块集合。降级轮廓最多生成 4096 个方块，超限时保留控制点。限制仅应用于客户端预览，最终放置仍使用原生成流程。

`check` 通过，新增测试覆盖平面阈值两侧、大体积和小结构。扫描估算不是精确方块数量，也不构成生成耗时上限。超限后的解析轮廓、边界和密度显示仍待完善，中等结构网格重建及实机性能采样仍未完成。

## 外壳边线复用 GPU 缓冲

快速起形的已确认和待确认外壳边线分别持有静态顶点缓冲。网格变化时重新上传，遮挡与可见绘制共用顶点，透明度在绘制时更新。上传前使用局部坐标，避免将大世界坐标直接转成浮点数。替换网格、改用几何轮廓和清理会话时释放缓冲，上传失败时释放新缓冲。

`check` 通过。当前没有取得 GPU 绘制实测证据，仍需检查远坐标、颜色、透明度、遮挡、重复取消和资源释放。面片仍使用原绘制路径，分级预览和帧时间验收未完成。

## 稳定预览复用外壳方块集合

外壳绘制按不可变渲染层及控制点位置缓存已确认、待确认和相邻形状范围。稳定帧复用这三个集合，候选点或渲染层变化时重建，清理渲染缓存时一并释放。颜色与方块状态仍由原外壳网格缓存检查。

`check` 通过。测试覆盖稳定帧复用、移动候选点后移除旧位置、输入集合修改不影响旧结果、新渲染层失效和会话清理。增量生成时的全量网格重建、顶点上传和实机帧时间仍待处理。

## 预览增量收取预算

快速起形每次收取增量结果限制为 4096 个方块，未收取的分区留在队列。通用接口按完整分区消费，允许首个分区超过调用方预算以避免无法前进。原有无限制消费入口保留给其他调用方。

`check` 通过。新增测试验证跨三次消费仍完整保留所有目标，并拒绝无效预算。这仅限制增量收取量，尚未消除累计集合复制、外壳网格重建和每帧顶点提交成本，不代表分级预览或帧时间验收完成。

## 当前 TODO 归档复核

从当前 TODO 移除已完成的版本化历史格式与管理器接入任务。批次保存维度、operation ID、方块、流体和方块实体数据，按存档与玩家 UUID 隔离，使用注册名编码。undo/redo 顺序索引已接入实际历史管理器。

后台保存、失败重试、服务器总磁盘配额和单玩家配额配置已完成，具体实现与测试证据见下方记录，不重复归档逐项开发过程。当前 TODO 只保留这些任务的剩余部分：世界与历史提交的崩溃一致性、保留期限、管理员清理入口及完整故障验收。

本次仅整理文档，没有新增代码或实机验证。磁盘历史按需加载、缓存淘汰、重启后撤回和观察者中键问题的实机调查仍未完成。下方记录中的“尚未完成”描述对应各次提交时点，以当前 TODO 为剩余工作依据。

## 瞬时堆压力不再永久拒绝小操作

内存预检现在区分请求本身超过最大安全工作集与当前堆暂时不足。后者进入软压力，预约仍按当前容量失败，放置任务通过现有预约状态等待重试。同步工作区展开与历史压缩入口使用当前容量检查，避免把可以等待误解为可以立即分配。

`check` 与 41 项 GameTest 通过，补充的定向测试也通过。测试模拟 1、3、16 格在高堆占用时等待、预约失败、压力下降后可继续；超过最大安全工作集仍拒绝。该缺陷能解释小请求的误拒绝，但尚未取得观察者中键线框完整交互的实机复现与堆采样，因此原调查条目保留。

## 停服前重试失败历史

正常停服在清空历史管理器前，为保存失败或仍有排队保存的玩家提交最终内存快照，然后等待历史 I/O。最终快照排在该玩家已接收任务之后，避免未处理的失败回调让最后状态漏存；不等待日常重试的 100 tick 间隔。

`check` 与 41 项 GameTest 通过。新增测试阻止索引发布、观察失败、移除故障，再通过停服保存入口提交内存历史并核验磁盘内容。无头服务器使用加大的 tick 上限等待真实磁盘回调。永久磁盘故障、保存队列拒绝或停服超时仍不能保证最终保存成功，跨世界提交的恢复协议尚未完成。

## 正常停服等待已接收的历史保存

服务器停止时等待该服务器已接收的完整历史保存链，最多 30 秒。等待包含后续批次、索引和清理步骤，不只等待当前 I/O 队列中的一个空任务；超时或中断记录错误，中断标记保留。

`check` 与 40 项 GameTest 通过。实际保存测试先阻塞历史 I/O，确认等待对象尚未完成，再释放线程，核验文件发布与等待完成。它只证明已接收工作不被提前视为结束；已经失败、尚未重新入队的历史，超时后的进程退出和断电仍需进一步处理，不代表停服历史完整性已经全部完成。

## 后台历史保存阶段部署到 .233

代码版本 `28e2568` 已通过 `deployTo233`（包含 `check`）部署到 `.233`。构建与目标 JAR 的 SHA-256 均为 `4281F586D8AB8CB0DCB6D62C07869B018EF0DF299FA998F14BE9EBD556A2DB95`。本次包含后台历史保存、独立 I/O 队列、保存结果提示、磁盘配额配置和清理候选启动恢复。

部署不代表整个 TODO 完成，也没有启用重启后的磁盘历史撤回。当前版本的客户端体验与首块延迟仍需实机验收。

## 启动扫描离线玩家清理候选

服务器启动后在独立历史 I/O 队列扫描存档中的玩家目录，逐个恢复 `retired.dat` 候选，不要求玩家上线。扫描只接受规范 UUID 目录，当前历史索引仍决定哪些批次受保护。一个玩家索引或候选损坏时记录失败并保留文件，继续处理其他玩家。

`check` 与 40 项 GameTest 通过。新增存储测试覆盖离线候选清理、单玩家索引损坏不影响其他玩家、失败候选保留；GameTest 启动执行了新生命周期入口。实际进程在清理各阶段中断的验证仍待完成。

## 清理候选落盘

更新历史索引前，先将清理候选写入玩家目录的 `retired.dat`，使用版本封装、CRC 和原子替换；候选数量与文件占用受配额限制。清理时合并磁盘候选并重新检查当前索引引用，全部成功后删除候选文件。新索引发布后清理入队失败，候选也已保存在磁盘。

`check` 与 40 项 GameTest 通过。新增测试重新创建存储实例，验证索引更新后可以继续清理，以及只有候选落盘、索引尚未更新时不能删除仍被引用的批次。这不是实际断电测试；启动时主动发现离线玩家候选、保留期限和世界/历史提交一致性仍未完成。

## 保留已执行清理任务的失败候选

清理任务在 I/O 队列开始执行后保留候选 UUID，执行失败不会丢弃；之后索引保存即使没有新增候选，也会重试。成功后释放候选集合。当前索引引用仍受保护。

`check` 与 40 项 GameTest 通过。新增测试破坏索引令清理失败，确认文件仍在，修复索引后以空候选重试，验证旧文件删除、当前文件保留及再次重试无重复工作。候选目前仅存内存，进程崩溃和队列拒绝前的候选持久化仍待完成，未标记完整清理恢复要求完成。

## 历史配额服务器配置

使用 NeoForge SERVER 配置注册 `fastformer-server.toml`，`history.playerDiskMiB` 默认 1024，`history.totalDiskMiB` 默认 8192。实际后台存储读取这些配置；调整后需重启，不因降低上限直接删除旧文件。用法见 `docs/history-storage.md`。

`check` 与 40 项 GameTest 通过，检查了实际启动生成的 TOML 文件及默认值。保留期限、全局历史缓存预算、磁盘加载和重启恢复仍未完成。

## 单玩家历史磁盘配额

实际后台存储增加单玩家 1 GiB 上限，服务器总量仍为 8 GiB。该玩家目录中的批次、索引和其他现存文件计入占用；索引替换按净增长检查，超限时不替换旧索引。缩小索引仍可执行，便于后续清理。

`check` 与 40 项 GameTest 通过。新增存储测试覆盖批次和索引恰好用完配额、后续写入拒绝、另一个 UUID 仍可保存、等大小索引更新，以及索引扩容被拒绝后原顺序可读取。配置入口、保留期限、全局缓存预算和完整清理故障恢复仍未完成。

## 清理已退出历史索引的批次

更新历史索引时比较前后 undo/redo 引用，新索引发布成功后才定向清理已移除批次。新操作清空 redo 或历史裁剪不再必然留下旧文件，待发布新批次不在清理候选中。

`check` 与 40 项 GameTest 通过。新增测试先将旧操作转入 redo，再发布新操作并清空 redo，检查旧文件已删除且当前 undo 文件保留。索引更新后、清理前崩溃留下的孤立文件，以及保留期限和磁盘清理失败后的候选恢复仍需后续处理，整体清理 TODO 未完成。

## 保存重试使用流式校验

保存重试检查既有批次时使用最多 8 KiB 缓冲区读取并校验 CRC，不再为检查文件是否有效而加载完整 payload。版本、长度、截断、尾随数据和损坏仍会拒绝；未找到文件时才发布缺失批次。

新增测试覆盖跨三个缓冲区的损坏、版本和预算不符、截断、尾随数据及空 payload。`check` 与 39 项 GameTest 通过，包括实际索引失败后重试。编码和缓存的整体内存预算仍待完成。

## 历史保存结果提示

后台历史保存首次失败时通知在线玩家：世界修改仍在，历史尚未保存到磁盘，将自动重试。失败状态保留到实际保存成功，重试开始不会清除此状态，因此连续失败不会重复发送同一提示；成功后发送保存完成提示。提示通过服务端主线程和现有消息入口发送，旧 owner 的回调不更新新 owner。

`check` 与 39 项 GameTest 通过，包含真实索引写入失败后的重试。客户端消息实际显示顺序仍待验收，未勾选整体提示验收条目。

## 后台历史不占用首写 journal 线程

后台历史编码和存储使用独立的单线程及有界队列，队列满时拒绝排队，不在服务端主线程执行。它不再与落块前必须完成的 journal 写入共用线程，避免大历史编码直接阻塞新操作的 journal 就绪。

新增测试阻塞历史线程，并验证 journal 线程仍能执行任务，最后释放并等待历史任务完成。`check` 与 39 项 GameTest 通过。这只证明任务队列隔离，不能替代磁盘竞争、CPU 负载下的首块延迟实测。

## 完成操作后的后台历史保存

历史管理器在接受完成批次后异步保存批次和 undo/redo 索引，撤回/重做完成与历史数量设置变化后更新索引。无 operation ID 的批次先分配稳定 ID。保存按玩家排序，编码队列限制待处理字节和操作数；调用方取消 Future 不会取消底层工作。失败后保留现有内存历史，间隔 100 tick 重试当前历史快照。

`check` 与 39 项 GameTest 通过。新增测试从真实管理器提交入口检查后台文件内容；使用非空目录阻止索引发布，确认内存历史仍在，移除故障后等待自动重试并核验 before/after 内容。

这仍是持久化接入阶段：没有启用磁盘历史恢复和缓存淘汰。世界 journal 与历史索引的跨文件恢复一致性、分段加载、整体内存预算和断电测试尚未完成，原 TODO 保持未完成。该实现不会在首次落块前等待历史文件保存，但实际延迟仍需采样。

## 减少历史文件 I/O 的完整副本

`c329e2b` 改为直接写入封装头部与现有 payload；文件读取在验证头部和预算后分配一次 payload，不再先读取完整封装并经过多次 defensive copy。CRC 与版本格式不变，测试逐字节比较旧编码和新文件输出，并覆盖读取预算及损坏数据。`check` 和补充的封装定向测试通过。

这减少文件 I/O 路径的临时副本，不代表 NBT 编码、解码与历史缓存的整体峰值已经有界；后续接入仍需要预约与分段。

## 部署恢复状态修复到 .233

代码版本 `b1e8786` 已通过 `deployTo233`（包含 `check`）部署到 `.233`。构建包与目标包 SHA-256 一致：`A43F0EE549D3D7EBB01BDAC84AF5AADE0A7A624624C578A6998D25B509ED405A`。本次部署包含取消恢复的状态简化和历史磁盘基础代码；磁盘历史尚未接入实际操作流程，不能据此宣称重启后历史可恢复。

## 删除历史恢复的手动暂停状态

历史管理器删除手动暂停恢复、等待用户恢复的分支及对应状态字段。重复取消不会停止活动恢复，也不会暂停尚未准备完成的恢复队列；普通撤回/重做仍可请求取消。原暂停测试改为验证自动继续和离线保留。`check` 与 37 项 GameTest 通过。

当前 Q 输入路由没有直接调用被删除的旧暂停入口，因此这次修改不宣称解决所有客户端 Q 交互问题；它消除了管理器中与退出语义冲突的状态。实机退出与恢复显示顺序仍待验收。

## 历史文件读取与定向清理

历史文件读取使用同一个已打开的文件通道检查大小并读取，分配大小不随文件增长而扩大，继续验证版本、长度和校验和。增加显式清理入口：只处理调用方指定的废弃批次，并保留当前 undo/redo 索引中的批次；未列入清理范围的新批次不会被删除。索引缺失或损坏时拒绝清理。

`check` 通过。测试覆盖读取预算、截断、尾随字节、校验和错误、保留 undo/redo 与待发布批次，以及索引故障下保留文件。尚未接入实际历史管理器，保留期限和自动清理策略仍未完成。

## 历史批次与顺序索引

增加按玩家 UUID 保存的独立批次文件和 undo/redo 顺序索引。批次禁止覆盖；索引更新前检查引用存在且不重复，失败时保留旧索引。写入队列限制字节数和操作数，满队列不复制批次数据，调用方取消不释放尚在写盘的数据额度。索引解码先检查文件剩余长度，避免伪造条目数触发大数组分配。

`check` 通过。测试覆盖顺序往返、重复发布、缺失引用、排队限额、取消期间的额度与副本隔离，以及批次发布后未更新索引时重新打开存储仍保留旧顺序。重新打开存储测试不等同于断电测试；尚未接入实际历史管理器，也未实现清理、分段与缓存淘汰。

## 单批次历史文件编解码

增加实际 WorldChangeBatch 的字节编解码，限制编码输出大小和 NBT 解析内存，拒绝截断及尾随数据。文件往返测试使用新的存储实例读取保存结果，检查维度、operation ID、方块、流体及方块实体数据，并验证玩家 UUID 隔离。`check` 和 37 项 GameTest 通过，代码提交 `4cc638f`。

该测试不执行服务器重启后的撤回，也不证明历史缓存有界。批次索引、实际历史管理器接入、分段和缓存淘汰仍属于未完成任务。

## 后续复核修正：历史内存与持久化

此前记录的“全局历史预算已完成”不成立。原实现只在离线时淘汰 owner，不能保证所有在线历史的总量有界，并且会丢弃尚未落盘的唯一历史副本。`b88d4a5` 已禁止淘汰非空历史，只清理空闲且没有历史或恢复工作的 owner；多玩家历史内存上限仍待磁盘接入后完成。

磁盘基础现有版本封装、CRC 校验、原子文件替换、按 UUID 的异步保存/读取、磁盘配额与排队 payload 字节限制。定向测试覆盖损坏、截断、文件替换、异步顺序、取消及写入失败后的额度释放。这些测试尚不证明真实 WorldChangeBatch 的持久化、重启撤回或完整内存预算已经完成。

此前把完整历史测试矩阵和提示状态验收勾选完成的记录也过早；相应原要求已恢复为未完成。后续应以实际接入和对应验收证据为准。

## 分段写入与实际任务回归

- [x] 普通放置遇到全部目标已经相同的情况时正常完成，清理未使用日志，不再把空历史当作提交失败。`check` 与 25 项 GameTest 通过。

- [x] 分开分段容量与实际切片长度，修复每增加一个快照就触发一次分段写盘的问题。首段容量为 256，后续为 4096。
- [x] 最终状态修正读取全部分段，避免后续分段的方块被误判为不在日志内。封口过程逐段读取元数据，不同时保留全部分段内容。
- [x] 选区复制不再为未修改的源方块写入恢复记录。日志预测只处理当前分段中的位置。
- [x] 选区清除与放置分别持有游标，进入放置阶段时重置放置游标。真实世界测试覆盖 1、16、256、512 方块复制和重叠移动。
- [x] 首次入队和首段日志完成后请求主线程继续服务。旧任务、取消任务和已消费结果不会由迟到回调再次写入。
- [x] 普通放置、工作区和选区取消后等待尚未结束的日志追加，恢复才能读取交出的快照。测试覆盖三类任务的交接等待。
- [x] 删除客户端草稿点后更新范围，恢复反向范围时规范化 min/max。提交状态拒绝空闲和未确认选区，保留普通放置、几何确认和工作区确认入口。

- [x] 普通放置使用剩余方块数量判断写入完成，清理暂存后仍能继续异步提交，不会占住写入许可。GameTest 覆盖普通放置和工作区各 1、16、256 方块成功，以及首块写入后的取消恢复。
- [x] 重叠移动直接写入目标最终状态，不先把重叠区清为空气。真实任务停在源区清除后，公开恢复入口可以还原未封口日志，不会把临时空气误当作外部修改。

验证记录：`check` 通过，733 个单元测试通过；24 项 GameTest 全部通过。该记录不代表首块可见延迟、实际进程重启或整个 anti-failure 验收完成。

- [x] 取消生成使用生成器的协作取消信号，并等待 worker 实际结束后释放预约。阻塞 supplier 测试验证取消期间仍计入内存，成功路径继续把预约交给放置任务。
- [x] 普通放置后续快照失败或内存拒绝统一交给恢复流程；选区和工作区的内存拒绝、超限也不会尝试提交部分结果。普通管理器回归测试验证已有写入转入恢复，并保留原方块快照。
- [x] 补齐工作区和选区首次 journal 就绪指标。

开发留档约定：阶段验证通过后提交本地 Git。anti-failure 等完整功能版本完成后再推送远端。

- [x] 普通放置遇到临时内存预约竞争时保留任务并等待重试；只有无法保留安全工作集时才硬拒绝。测试验证释放竞争预约后同一任务可以继续。
- [x] 分段启动恢复逐段校验和读取，不再同时保留全部解码分段。写入前检查所有分段与修正记录，未封口日志继续逆序回退。
- [x] 普通放置、工作区和选区释放工作集预约时，等待日志创建、追加及历史发布实际结束。测试覆盖未完成 begin、append 和 publication，等待不会取消历史发布。
- [x] 启动恢复 GameTest 覆盖公开扫描入口、旧格式与分段日志、保存失败后保留日志及重试、损坏日志和未加载维度阻止写入。真实进程中断与重启仍需独立验收。

本阶段验证：`check` 通过，`runGameTestServer` 的 16 项必需测试全部通过。生成 worker 取消后的预约释放，以及工作区/选区的临时内存竞争仍有后续任务。

- [x] 从 `.233/logs/debug-2.log.gz` 定位 2026-09-10 01:11 的 `system_chat` 断线：684 方块操作失败后，状态消息直接传入 UUID 翻译参数。消息入口现在将不支持的对象转成有长度限制的字符串。GameTest 验证原始 UUID 被拒绝，清理后的失败消息可编码往返；`check` 与 GameTest 通过。实际复杂选区复测另列。

- [x] 写入事务不再持有历史提交对象。普通放置、工作区和选区任务分别管理历史发布，恢复交接等待异步发布停止。更新所有权及恢复交接测试，`check` 和 GameTest 通过。
- [x] 选区应用按请求 ID 去重；普通提交发送失败时按请求 ID 解除等待；世界会话结束时清除路径和操作点双击记录。输入状态机和请求去重定向测试通过。

- [x] 删除嵌入放置上下文中的出射面反向追踪，保留首次命中的位置和平面。六方向 GameTest 检查嵌入和表面模式的朝向输入一致。`check` 与 GameTest 通过，预览和最终方块朝向另列实机验收。

## 首段唤醒与提交预约

- [x] 选区用有界队列保存已检查的目标，首段日志完成后直接消费该队列，不再等待所有目标检查结束。队列保留内存重试前捕获的目标，避免重试丢块。
- [x] 准备阶段用尽预算后，允许一个已写入日志的首块执行；每个预算最多追加一个单位，不提高全局预算，冲突检查和日志门禁仍然生效。
- [x] 真实任务测试覆盖工作区和选区 1、16、256、512 方块的日志回调唤醒，普通放置覆盖 1、16、256 方块的管理器唤醒。工作区和选区还覆盖一单位预算及取消后的迟到日志完成。
- [x] 三类任务均覆盖提交预约暂时竞争后重试成功、硬拒绝和预约释放。工作区与选区增量快照重试不会重复累计快照预算。

本阶段验证：`check` 通过，740 个单元测试通过；`runGameTestServer` 的 28 项必需测试全部通过。客户端首块可见时间、进程重启及完整异常/卸载矩阵仍有独立待办。

## 输入所有权与暂存释放复核

- [x] 快速替换的交互事件与 tick 路径统一，只能由输入状态机的 VANILLA 路由执行。已有建筑、几何和选区会话不能被快速替换抢占。
- [x] 生命周期测试检查三类任务在提交前释放生成结果、目标或源数据及 expected 暂存，提交完成后释放 before/after 快照和提交对象；预约恢复到测试前基线。
- [x] 复核选区绘制、面命中、重复缩放和控制器变换使用当前 bounds；原始输入点不作为范围角点。自动化已覆盖推拉保留输入点、扩展范围、删除点重算、反向范围规范化和工作区轮廓。画面与连续交互另列实机验收。

预览异步生成的真实完成竞态、输入分支后续收敛、异常/卸载/重启矩阵仍未完成；本次不勾选这些条目。
`check` 通过，741 个单元测试通过；28 项 GameTest 全部通过。已部署到 `.233`，构建与部署 JAR 的 SHA-256 一致。

## 异步预览、写入异常与维度等待

- [x] 将异步预览的 key 和 Future 放入同一个所有权对象。替换或取消时解除旧 Future；只有匹配当前 key 的结果能被消费一次。4 个单元测试覆盖换 key、错误 key、旧结果先完成和重复消费。
- [x] 三类真实任务在箱子移除回调抛异常后恢复箱子及 3 个钻石，原复制源不变，任务、内存预约和写入许可均正常释放。异常来自实际 NeoForge 实体加入回调，无生产故障注入开关。
- [x] KEEP_EXISTING 重叠移动在两次源区清除后取消；交替方块恢复正确，外部修改保留。工作区在两个写入后发生已验证目标冲突，也能恢复任务拥有的写入并保留外部修改。
- [x] 三类任务先实际写入一个方块，再经管理器的维度不可用分支等待，保留任务并解除活跃预约；重新提供真实维度后原任务完成且释放写入许可。测试使用不可用上下文，不宣称实际卸载或进程重启已完成。

验证：`check` 通过，745 个单元测试通过；32 项 GameTest 全部通过。生成前预约竞争仍存在直接拒绝分支，已记入后续子任务。

## 生成预约等待与冻结输入

- [x] 普通生成的两个分支和几何生成入口在临时预约竞争时保留任务，后续 tick 重新预约；成功后只启动一次异步生成。只有当前工作集硬拒绝才退出。
- [x] 取消和关服会丢弃未启动的生成输入。已经启动的 worker 不以 Future 取消代替实际停止，预约保持到 worker 完成。
- [x] 内存硬拒绝优先于空操作提示，避免清空目标后错误显示“空操作”。
- [x] 墙体和复合形状在确认时冻结点位、挤出和形状参数，延迟生成不再读取可变会话。木框架解析只捕获原型和配置，不保留整个玩家上下文。
- [x] 异步生成调度失败时释放尚未交给 worker 的预约。

验证：`check` 与 32 项 GameTest 通过；753 个单元测试通过。新增测试覆盖等待、只启动一次、等待取消、worker 退出后释放、硬拒绝、管理器取消/关服，以及两类几何参数冻结。客户端内存等待交互另列实机验收。

## 输入动作路由与手势结束

- [x] 键盘、原版交互、鼠标按钮的业务分支使用事件入口取得的同一状态机路由；滚轮工作区动作也要求 OPERATION 所有权。状态机统一允许或拒绝新选区、粘贴动作，防止建筑/几何会话开启另一选区。
- [x] Alt 修改选区控制器只发生在选区会话。Alt 新建选区入口不能绕过状态机。
- [x] 鼠标手势记录拥有它的按键，只接受该按键的一次释放；旧手势、其他按键和重复释放不会完成新手势。没有匹配 FastFormer 手势的释放事件留给原版处理。
- [x] 结束会话时同时解除原版攻击和使用键，避免任务结束后继续执行旧的按住动作。

验证：`check` 通过，755 个单元测试通过；本次仅客户端输入修改，之前服务端生成阶段的 32 项 GameTest 记录仍单列，不当作客户端交互验收。纯画面、性能采样和大型操作实机条目已按用户要求保留原文勾选，新增未完成实机验收条目；这些勾选不代表采样或实机结果已取得。

## 失败终态与真实进程重启

- [x] 三类真实任务在 journal 调度拒绝时不写入世界、不创建无用恢复，并释放预约和写入许可。
- [x] 三类任务的历史发布结果失败后，通过真实管理器创建恢复，还原原始方块并释放资源。测试替换已完成的发布结果，不中断活跃 worker；它证明管理器的失败处理，不模拟压缩器内部故障。
- [x] 停服资源测试覆盖普通放置、工作区和选区：管理器清空任务后，预约仍等日志 I/O 与历史压缩真正结束才释放。该测试控制 Future 和压缩读取边界，不代替磁盘故障测试。
- [x] 独立服务器进程在主世界、下界、末地分别运行三类任务，确认首块已改变、manifest 和首段文件存在且未封口，保存世界后以退出码 17 强制结束。第二个进程经真实启动事件恢复原方块，确认复制源不变、日志清空、写入门开放、三维度许可空闲及预约归零，返回 `PASS`。
- [x] 增加 `scripts/Test-RecoveryRestart.ps1`。测试使用专用 `run/recoveryProcessTest`，已有目录时拒绝覆盖，两个服务器顺序运行。硬退出测试类从生产 JAR 排除，`verifyModJar` 检查它没有进入发布包。

验证：`check` 通过，758 个单元测试通过；本轮 34 项 GameTest 通过。真实进程测试通过，日志确认三个分段 journal 各恢复一个位置。首批之后的多段崩溃时点、实际客户端断线及同进程世界卸载仍单列实机验收，不宣称整个 antifailure 功能验收完成。

已通过 `deployTo233` 部署到 `.233`，构建与部署文件 SHA-256 均为 `94C2A5C92E2A2E532AD4250C183168D7AB8D25CF2764C9CAE2EDEB71F713D0F4`。当前 TODO 剩余 25 项均为实机验收或测量；旧条目只改勾选状态，正文未改。


## TODO 整理

`current_todo.md` 已清理已完成条目，仅保留未完成的实机验收、性能采样、会话恢复、预览优化、历史磁盘保存和 anti-failure 后续任务。已完成实现、自动化测试和部署证据保留在本归档及此前主题记录中。


- [x] 墙体和复合形状的延迟生成使用确认时冻结的点位、挤出和形状参数；`DeferredGeometryBuildTest` 覆盖两类工作流。


- [x] 为离线历史 owner 增加全局内存防护：总历史缓存预算 1GiB，最多保留 128 个空闲离线 owner；仅淘汰无活动任务、恢复任务和待处理记录的 owner。新增生命周期测试验证忙碌恢复不会被清理。`check` 通过。


- [x] 增加 `WorldHistoryOwnerLifecycleTest`，验证多 UUID 离线历史受全局预算和 owner 数量限制，活动恢复 owner 不会被淘汰。磁盘历史加载与格式兼容仍待实现。


- [x] 失败提示路径已按等待、内存拒绝、维度不可用、恢复重试和恢复阻断分别使用状态对应消息；已有网络编码回归覆盖长字符串、非有限值和 UUID 参数。客户端措辞顺序仍需实机复核。
