# TODO

只记录未完成且可验收的工作。

- [ ] 游戏内验收倾斜圆柱 / 锥柱台体连续体积修复：`SOLID` 无内部孔洞，`HOLLOW` 与 `SOLID` 外表面一致。
- [ ] 游戏内验收锥柱交互：整体 Gizmo、轴线拖拽、短按左键回退、模式记忆和新版 HUD。
- [ ] 游戏内验收 Alt 反色准星：平滑圆环、嵌入/半格形状，以及后处理失败时的 GUI 回退。
- [ ] 等用户确定复杂形状页面的曲线、截面、扫掠和放样交互后，再替换旧复合原型。
- [ ] 冻结更新：为预览和放置过程提供明确的冻结/实时更新控制，避免状态在操作中意外漂移。
- [ ] 方块与玩家位置响应：让方向性方块等状态根据玩家位置和朝向实时计算，例如木头的朝向。
- [ ] 自动楼梯线：根据连续路径和高差生成可预览、可放置的自动楼梯线。
- [ ] 撤回：统一世界操作历史、任务中断、断线/停服收敛和崩溃恢复日志已接入；待游戏内验收大型结构、方块实体、异步冲突、中途退出和崩溃重启恢复。

## 空手选区（需求记录，暂不实现操作执行）

- [ ] 空手选区会话，阶段沿用快速起形的点 / 线 / 面节奏，Q 退出，Enter 确认选区。
- [ ] 长方体路线：AABB 两点选区；左键、右键选择点 1 / 点 2；中键临时扩展；对着朝向面左键收缩、右键扩展；长按左键或右键持续调整；Gizmo 只允许整体位移和缩放，不允许旋转。
- [ ] 棱柱路线：沿用快速起形多边面输入；不做棱锥；Gizmo 支持整体位置、控制点位置和顶面位置调整；不允许缩放。
- [ ] 选区预览渲染：棱柱附近的边只显示相邻棱方块的边，其余区域优先显示面；具体视觉方案待验收。
- [ ] 选区确认后的操作执行暂不实现，等待用户决定。

## 用户关注点（仅记录）

以下内容是用户自己的规划索引，不构成实现授权。未经用户明确确认，AI 不得自行拆分需求、选择方案、改变语义或开始实现。

- 倾斜房子墙体/屋顶（用户已接近解决）。
- 道路扫掠/放样。
- 几何体大楼（多面体，用户已解决）。
- AABB 选区误伤其他建筑。
