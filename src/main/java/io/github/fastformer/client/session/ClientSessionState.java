package io.github.fastformer.client.session;

/** The mutually exclusive top-level client session modes. */
public enum ClientSessionState {
   EMPTY,
   QUICK_SHAPE,
   SPECIAL_SHAPE,
   SPECIAL_ITEM
}
